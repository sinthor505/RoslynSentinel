[
  {
    "name": "get_call_graph",
    "description": "Builds a forward call graph from a method: shows what that method calls, what those callees call, and so on up to maxDepth levels (default 3). Only follows calls into methods with source locations in the solution (not BCL/NuGet). Returns a CallGraphNode tree: MethodName, ContainingType, FilePath, Line, Callees. Already-visited methods appear as leaf nodes to prevent cycles.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        },
        "maxDepth": {
          "type": "integer",
          "default": 3
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_inconsistent_async_suffix",
    "description": "Finds async methods not ending with 'Async', and non-async methods that end with 'Async'. Excludes event handlers.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_large_methods",
    "description": "Finds methods exceeding a line count threshold across the solution or a specific project. Returns MethodName, TypeName, FilePath, and LineCount. Methods over 50 lines are too large to modify safely without reading in full — they are also prime extract-method candidates. Default threshold is 50 lines.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "maxLines": {
          "type": "integer",
          "default": 50
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_internal_classes_that_could_be_private",
    "description": "Identifies internal classes that are only used in a single file and could be made private, optionally filtered by project.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "sort_and_deduplicate_usings",
    "description": "Sorts using directives (System.* first, then alphabetical) AND removes exact\nduplicates in a single operation, writing the result directly to disk.\n\nFILLS GAP between two standard tools:\n  • sort_usings     → sorts but does NOT remove duplicates\n  • remove_unused_usings → removes unused usings, but a duplicate that is\n                           \"used\" (both copies resolve) won't be removed\n\nThis tool handles the case where a file legitimately has two identical using\ndirectives (e.g., a merge conflict or copy-paste artifact) and you want both\nsorted AND deduplicated in one step.\n\nParameters:\n  filePath    — absolute path to the .cs file to clean\n  writeToFile — true (default) = writes sorted/deduped content to disk immediately\n                false = preview mode, returns UpdatedContent without writing\n\nReturns: OriginalCount, RemovedDuplicates, UpdatedContent, WrittenToDisk.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "writeToFile": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "read_plan",
    "description": "Reads a migration or refactor plan file from the solution's docs/plans/ directory. Only bare filenames are accepted; only documentation extensions are permitted.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filename": {
          "type": "string"
        }
      },
      "required": [
        "filename"
      ]
    }
  },
  {
    "name": "propagate_cancellation_token_in_file",
    "description": "Propagates CancellationToken parameters to all eligible call sites across all methods\r\nin a file. For each method that has a CancellationToken parameter, applies the same\r\nlogic as propagate_cancellation_token_in_method. Methods without a CT parameter are\r\nskipped silently. Returns aggregated per-method results and a staged change-set.\r\n\r\nParameters:\r\n  filePath    — absolute path to the source file.\r\n  methodNames — optional array of method names to restrict processing (null = all eligible).\r\n  autoStage   — when true (default), stages the changes and returns a ChangeId.\r\n\r\nReturns PropagateCtFileResult with:\r\n  PerMethod        — one PropagateCtResult per method processed.\r\n  TotalForwarded   — total call sites rewritten across all methods.\r\n  TotalSkipped     — total call sites skipped across all methods.\r\n  MethodsProcessed — count of methods that had a CT param and were processed.\r\n  MethodsSkipped   — count of methods without a CT param (or filtered out).\r\n  ChangeId         — staged change id (null if autoStage=false or no changes).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodNames": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string null"
          },
          "default": null
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_object_creation_sites",
    "description": "Finds all object creation sites (new T(...) and implicit new(...)) for a given type name substring match, optionally scoped to a file or project. Returns file path, line, column, resolved type name, containing method, and argument count. Set sortByFrequency=true to rank results by how often each resolved type is instantiated — useful when the typeName is broad (e.g. 'Exception') and you want the most-created types first. Supports both explicit 'new Foo()' and implicit 'new()' syntax with type inference from context.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "typeName": {
          "type": "string"
        },
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "sortByFrequency": {
          "type": "boolean",
          "default": false
        }
      },
      "required": [
        "typeName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "convert_property_to_methods",
    "description": "Converts a property into formal GetX() and SetX() methods.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "propertyName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "propertyName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_unawaked_dispose",
    "description": "Detects DisposeAsync() calls that are not awaited. In a synchronous Dispose() method or any context without await, the ValueTask returned by DisposeAsync() is discarded — async cleanup (file flushes, connection teardown) finishes after the method returns, leaving resources dangling.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "make_method_thread_safe",
    "description": "Adds a private lock object field and wraps a method body in a lock statement. Specify lockFieldName if '_lock' is already used for another type. Returns the updated source as a string. Does NOT write to disk or update the workspace. Pass the result to apply_proposed_changes to save.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        },
        "lockFieldName": {
          "type": "string",
          "default": "_lock"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_project_framework_summary",
    "description": "Returns a summary of every project's TargetFramework value. Use this before check_project_consistency to see the full framework landscape across the solution.",
    "inputSchema": {
      "type": "object",
      "properties": {}
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_value_type_mutation_intent",
    "description": "Flags methods that reassign a parameter inside the method body in a way that\r\nis invisible to the caller. Two warning patterns detected:\r\n\r\nValueTypeParameterReassigned: A value-type parameter (int, bool, struct, enum)\r\nis reassigned but not returned — the caller's copy is unaffected. Use 'ref' or\r\nreturn the value if caller visibility was the intent.\r\n\r\nReferenceTypeParameterReplaced: A reference-type parameter is replaced with a\r\nnew instance (new ...) but not returned. The caller's reference still points to\r\nthe original object.\r\n\r\nParameters with 'ref', 'out', or 'in' modifiers are excluded. No auto-fix —\r\nthese require developer judgement about intent.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "check_for_unused_event_subscriptions",
    "description": "Scans a file for event subscriptions that are never unsubscribed, potential memory leaks.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "run_uplift_batch",
    "description": "Uplifts callers of an Asyncify-bridge sync wrapper to use the async overload directly.\r\nUses in-memory Roslyn compilation for validation — no MSBuild required.\r\nFor each caller of the named bridged method:\r\n  1. Bridges the caller (convert_to_async_bridge) to create callerAsync(CancellationToken).\r\n  2. Rewrites the callerAsync body: bridgedMethod(args) → await bridgedMethodAsync(args, ct).\r\n  3. If propagateCancellationTokens=true: propagates CT to all other async callees in callerAsync.\r\n  4. Validates in-memory.\r\n  5. On success: writes the file to disk.\r\n  6. On error: flags the caller [MigrationCandidate(\"NeedsManualReview\")] and continues.\r\n\r\nParameters:\r\n  bridgedMethodName              — name of the already-bridged sync method (e.g. \"search\").\r\n                                   The async counterpart is assumed to be bridgedMethodName + \"Async\".\r\n  projectName                    — restrict caller scan to one project (null = entire solution).\r\n  maxCallers                     — max caller methods to process this call (default 10).\r\n  dryRun                         — when true, reports what would happen without writing files.\r\n  propagateCancellationTokens    — when true (default), also propagates the CT parameter to\r\n                                   other async callees in the new async overload.\r\n\r\nReturns UpliftBatchResult with:\r\n  Uplifted         — callers successfully uplifted and written to disk.\r\n  Skipped          — callers that failed (each flagged NeedsManualReview where possible).\r\n  RemainingCallers — count of callers still pending after this batch.\r\n  StopReason       — \"batch_complete\" | \"budget_exhausted\" | \"no_callers\" | \"dry_run\".",
    "inputSchema": {
      "type": "object",
      "properties": {
        "bridgedMethodName": {
          "type": "string"
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "maxCallers": {
          "type": "integer",
          "default": 10
        },
        "dryRun": {
          "type": "boolean",
          "default": false
        },
        "propagateCancellationTokens": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "bridgedMethodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "format_document_safe",
    "description": "FORMAT_DOCUMENT_SAFE — Roslyn formatter with true preview support.\n\nFIXES MS BUG: The standard format_document tool has no preview parameter at all.\nChanges are always applied immediately. There is no way to see what would change\nbefore committing.\n\nThis tool fixes that by defaulting preview=true:\n  • preview=true  (default) → returns formatted content WITHOUT writing to disk\n  • preview=false            → writes to disk and updates the in-memory workspace\n\nUse preview=true first to verify the formatted output is as expected,\nthen call again with preview=false to apply.\n\nParameters:\n  filePath  — absolute path to the .cs file to format\n  preview   — true (default) = read-only, false = apply to disk",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "preview": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_services_not_registered",
    "description": "Scans all constructor parameters in the solution (or a specific project) and identifies injected service types that appear to be missing from the DI container registrations. Detects interfaces (IFoo pattern) and common service-like types (ending in Service, Repository, Manager, Factory, Provider, Handler, Validator, Dispatcher). Framework-provided types (ILogger, IOptions, IConfiguration, etc.) are excluded. Returns consumer class name, file, line, the missing type, and the parameter name. Use to catch unregistered dependencies before runtime failures.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "convert_to_pattern",
    "description": "Modernizes code using C# pattern matching (C# 7.0+).\nConverts patterns like:\n- 'if (x == null)' → 'if (x is null)'\n- 'if (x != null)' → 'if (x is not null)'\n- 'if (obj != null && obj.Property > 0)' → 'if (obj is { Property: > 0 })'\n\nfilePath: path to the .cs file to transform.\nReturns the updated file content with modern pattern matching applied.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "analyze_method_control_flow",
    "description": "Analyzes control flow for an entire method body using Roslyn's semantic analysis.\r\n\r\nUnlike the raw line-based analyze_control_flow tool, this takes a method name — no\r\nneed to count line numbers or worry about accidentally including the method signature\r\n(which causes a 'statements not within the same statement list' error in line-based tools).\r\n\r\nReturns: EndPointIsReachable (false = all code paths return/throw), ReturnStatements,\r\nThrowStatements, BreakStatements, ContinueStatements.\r\n\r\nIf the method has multiple overloads, supply disambiguateLine (any line number that\r\nfalls inside the desired overload's body) to select the correct one.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        },
        "disambiguateLine": {
          "type": [
            "integer",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "convert_to_source_generated_logging",
    "description": "Converts LogInformation/LogError/LogWarning calls in a class to high-performance [LoggerMessage] partial methods and makes the class partial.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_task_run_in",
    "description": "Detects 'await Task.Run(...)' patterns in server-side code. Task.Run wraps work in a new thread pool task which is wasteful in async server code. Prefer direct async methods.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_obsolete_callers",
    "description": "Finds all call sites that invoke a method decorated with [Obsolete].\r\nDesigned for tracking CS0618 migration progress — every result is a call site that still\r\nneeds to be migrated away from the deprecated (Asyncify-bridge) method.\r\n\r\nParameters:\r\n- messagePattern: Optional substring to filter by the [Obsolete] attribute message (case-insensitive).\r\n  Use \"Asyncify-bridge\" to restrict to bridge wrappers created by the async migration.\r\n- filePath: Optional; restrict results to call sites in this file.\r\n- projectName: Optional; restrict results to call sites in this project.\r\n\r\nReturns ObsoleteCallerFinding records: ObsoleteMethodName, ObsoleteMessage, DeclaringType,\r\nCallerMethod, CallerType, FilePath, Line, CodeSnippet.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "messagePattern": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "apply_cancellation_token_to_file",
    "description": "Adds 'CancellationToken cancellationToken = default' to all eligible async methods in a\r\nfile in a single Roslyn rewrite pass, then writes the result to disk and updates the\r\nin-memory workspace.\r\n\r\nEligible methods: async or Task/ValueTask-returning, no existing CancellationToken\r\nparameter, and not event handlers (fixed-signature delegates).\r\n\r\nmethodNames: optional array of method names to limit the scope. When omitted, all eligible\r\nmethods in the file are processed.\r\n\r\nReturns ModifiedMethods (changed), SkippedMethods (eligible async methods skipped for a\r\nspecific reason — already have CT, abstract, event handler, or outside the requested scope;\r\nsync methods are not included), TotalModified, WroteToFile, WorkspaceInSync, and\r\nWorkspaceVersion. If WorkspaceInSync is false, call load_solution before further analysis.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodNames": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string null"
          },
          "default": null
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_large_types",
    "description": "Finds types (classes, structs, records, interfaces) exceeding a line count threshold across the solution or a specific project. Returns TypeName, FilePath, and LineCount. Use before modifying a large class — anything over 500 lines is a prime extract-class candidate. Default threshold is 500 lines.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "maxLines": {
          "type": "integer",
          "default": 500
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "change_accessibility",
    "description": "Changes the accessibility modifier of a type or member.\n\ntargetName is the class/method/property/field name to modify.\naccessibility must be one of: \"public\", \"private\", \"internal\", \"protected\",\n\"protected internal\", or \"private protected\".\nUse autoStage=true (default) to get a ChangeId for ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "targetName": {
          "type": "string"
        },
        "accessibility": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "targetName",
        "accessibility"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "add_base_type",
    "description": "Adds a base type or interface to a type declaration.\n\ntypeName is the class/record/struct/interface to modify; baseTypeName is what to add\n(e.g. \"IDisposable\", \"BaseController\", \"IMyService<string>\").\nIf the type already inherits or implements baseTypeName, the file is returned unchanged.\nUse autoStage=true (default) to get a ChangeId for ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "typeName": {
          "type": "string"
        },
        "baseTypeName": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "typeName",
        "baseTypeName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_method_complexity",
    "description": "Calculates the cyclomatic complexity of a method: 1 + one for each if/else/case/while/for/foreach/catch/&&/||/?? branch. Returns the complexity score and the list of conditionals that contribute to it. Complexity guide: 1–4 = Low (easy to understand and test), 5–7 = Medium, 8–10 = High (refactoring candidate), >10 = Very High (split required). Use before modifying a method to gauge how risky the change is.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_async_void_without_try_catch",
    "description": "Detects async void methods whose entire body is not wrapped in a try/catch. Unhandled exceptions inside async void crash the process via the unhandled-exception handler — there is no way for a caller to catch them. Wrap the body in try { } catch (Exception ex) { }.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_structural_smells",
    "description": "Scans the solution for structural issues with granular filtering. typeFilter options: All, MultiType, NameMismatch.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "typeFilter": {
          "type": "string",
          "enum": [
            "All",
            "MultiType",
            "NameMismatch",
            "NameofCandidate",
            "LegacyGuardClause",
            "Verbosity",
            "ThreadSafety",
            "TimeAbstraction",
            "GenericException"
          ],
          "default": "All"
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_type_members_detail",
    "description": "Returns all members of a type (methods, properties, fields, events) with full metadata: signature, accessibility, kind, IsInherited, IsOverride, IsAbstract, IsStatic, file path, and line. Set includeInherited=false to show only directly declared members.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "typeName": {
          "type": "string"
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "includeInherited": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "typeName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "check_for_empty_catch_blocks",
    "description": "Scans for empty catch blocks. Optionally filtered by project.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "detect_anti_patterns",
    "description": "Detects common C# anti-patterns introduced by AI code generation: BlockingTaskWait (.Result/.Wait()/.GetAwaiter().GetResult()), AsyncVoidMethod (non-event-handler async void), StringConcatInLoop (string += in loops), CatchExceptionSwallow (empty catch blocks), DisposedObjectUsage (use after Dispose), MissingCancellationToken (public async methods with 3+ params lacking CancellationToken), MagicNumber (unexplained numeric literals). Optionally scope by filePath, projectName, or patternFilter array.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "patternFilter": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string null"
          },
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_file_diagnostics",
    "description": "Gets all compiler errors and warnings for a specific file.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "use_time_provider",
    "description": "Replaces direct DateTime usage with modern TimeProvider abstractions for testability.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "convert_to_background_service",
    "description": "Converts a class to a BackgroundService: adds BackgroundService base class, generates ExecuteAsync override, and adds Microsoft.Extensions.Hosting using directive.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_circular_dependencies",
    "description": "Identifies circular project references (A -> B -> A).",
    "inputSchema": {
      "type": "object",
      "properties": {}
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_string_magic_values",
    "description": "Finds string literal magic values: the same string appearing 3+ times across a file, project, or solution. Returns value, occurrence count, suggested constant name (PascalCase), and all file/line locations. Excludes empty strings, nameof() args, attribute arguments, and strings shorter than 3 chars.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "minOccurrences": {
          "type": "integer",
          "default": 3
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_regex_new_in_loop",
    "description": "Detects new Regex() construction inside loop bodies (for/foreach/while/do). Constructing a Regex per iteration recompiles the pattern on every pass — a pure waste for literal patterns, and a ReDoS amplification vector for variable patterns. Hoist to a static readonly field.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "add_cancellation_token_to_method",
    "description": "Adds 'CancellationToken cancellationToken = default' as the last parameter to a method\r\nand propagates it to async callees in the method body that have a CancellationToken\r\noverload. Also adds cancellationToken to Task.Delay() calls.\r\nReturns CancellationTokenResult where exactly one field is populated:\r\n  autoStage=true (default): ChangeId is set; apply with apply_staged_changes(changeId).\r\n  autoStage=false: Source is set; pass Source.UpdatedSource to apply_proposed_changes to save.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_workspace_health",
    "description": "GET_WORKSPACE_HEALTH — Targeted workspace health check that fixes false negatives.\n\nFIXES MS BUG: The standard diagnose tool reports healthy:false even when all projects\nload successfully, because it tests MSBuild path existence rather than actual workspace\nstate. A workspace with 86/86 projects loaded correctly can be falsely reported as\nunhealthy.\n\nThis tool reads actual solution state directly:\n  • IsOperational  — true if workspace itself is functional (not throwing)\n  • HasLoadedSolution — true if a .sln or .csproj is currently loaded\n  • ProjectCount/DocumentCount — actual loaded counts from the workspace\n  • LoadErrors — non-fatal warnings that occurred during solution loading\n  • Summary — human-readable status\n\nNote: IsOperational=true + HasLoadedSolution=false simply means no solution has\nbeen loaded yet — this is a normal state, not an error.",
    "inputSchema": {
      "type": "object",
      "properties": {}
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_multiple_enumeration",
    "description": "Detects IEnumerable<T> or IQueryable<T> locals/parameters that are iterated more than once without a materializing call (ToList/ToArray). Multiple enumerations can execute a database query or generator multiple times. Reports variable name and enumeration line numbers.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_duplicate_blocks_in_class",
    "description": "Finds duplicate statement sequences within the methods of a single class.\nUses structural hashing (SyntaxKind-based) so blocks match regardless of\nvariable names or literal values — the same control-flow shape is a clone.\n\nReturns groups of matching block locations including:\n• StatementCount — how many statements the clone spans\n• HasControlFlowExit — true if any statement is a return/break/continue/throw\n  (flag only; does not block the finding)\n• SnippetPreview — first 120 chars of the first statement\n• CapturedVariables — variables that would need to become parameters if extracted\n• ProducedVariables — variables that would need to be returned if extracted\n• Occurrences — method name, start line, end line, file path for each copy\n\nUse this to find Extract-Method candidates within a single class before the\nduplication spreads. Set minStatements lower (e.g. 3) for aggressive detection,\nhigher (e.g. 6) for only substantial clones.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        },
        "minStatements": {
          "type": "integer",
          "default": 4
        }
      },
      "required": [
        "filePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_async_migration_progress",
    "description": "Returns async migration progress statistics for the solution or a single project.\r\nReports: total async Task/ValueTask methods, how many already have a CancellationToken\r\nparameter (and how many still need one), percentage coverage, number of Asyncify-bridge\r\nwrapper methods ([Obsolete(\"Asyncify-bridge:...\")]), number of call sites that still\r\ninvoke those bridge wrappers (CS0618 sites pending migration), and count of async void\r\nevent handlers (informational — their signatures cannot be extended).\r\n\r\nprojectName: restrict statistics to a single project; null = entire solution.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_solution_metrics",
    "description": "Gets deep metrics for the entire solution or a specific project.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "validate_proposed_diff",
    "description": "Validates a proposed Unified Diff in-memory and returns compiler diagnostics.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "unifiedDiff": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "unifiedDiff"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "generate_test_scaffold",
    "description": "Generates an xUnit+Moq test class scaffold for a given class. Auto-detects constructor parameters to create Mock<T> fields, instantiates the SUT, and adds one test method stub per public method with Arrange/Act/Assert comments.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "simplify_verbosity",
    "description": "Simplifies redundant code patterns globally in a file (target-typed new, null-coalescing assignment, etc.).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "optimize_resource_disposal",
    "description": "Scans for IDisposable objects that are not properly disposed. Optionally filtered by project.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_cancellation_token_not_forwarded",
    "description": "EPC31: Finds async methods that have a CancellationToken parameter but call awaitable\r\nmethods ('*Async') without forwarding the token. Detects missed cancellation propagation\r\nthat leaves callers unable to cancel downstream work.\r\nfilePath: scan a single file. Leave null to scan the entire solution.\r\nReturns method name, file, line, the unforwarded callee name, and the CT parameter name.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_comprehensive_health_report",
    "description": "Generates a paged comprehensive health report. engines: Structure, Modernization, Performance, Safety, Architecture. offset/limit for project paging.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "engines": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string",
            "enum": "Structure Modernization Performance Safety Architecture"
          },
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "offset": {
          "type": "integer",
          "default": 0
        },
        "limit": {
          "type": "integer",
          "default": 10
        },
        "timeoutSeconds": {
          "type": "integer",
          "default": 25
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_task_void_usage",
    "description": "Detects dangerous 'async void' usage that can crash the application.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_boxing_allocations",
    "description": "Finds potential boxing allocations. Optionally filtered by project.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_types_by_attribute",
    "description": "Finds all TYPES (classes, interfaces, records, structs, enums) decorated with a specific attribute using the semantic model for accuracy. Unlike find_attribute_usages (which returns all targets including methods and properties), this returns only type-level decoration. Useful for: 'find all [ApiController] classes', 'find all [TestClass] types', 'find all [Serializable] types'. Returns TypeName, FilePath, and Line.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "attributeName": {
          "type": "string"
        }
      },
      "required": [
        "attributeName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "sort_members",
    "description": "Reorders members of a class/struct/record by convention: fields, constructors, destructors,\nproperties, indexers, events, methods, operators, nested types.\n\nWithin each category: static members come before instance members, then alphabetical by name.\ncontainerName is the type whose members to sort.\nUse autoStage=true (default) to get a ChangeId for ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "containerName": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "containerName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "detect_breaking_changes",
    "description": "Compares a previously captured API surface (baseline) against the current code and reports breaking changes: removed types, removed/renamed members, and signature changes. Workflow: (1) call get_public_api_surface_snapshot to capture the baseline, (2) make code changes, (3) call this tool with the baseline list. Scope with projectName or filePath as in step 1.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "baseline": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": "@{kind=; containingType=; signature=; filePath=; line=}",
            "required": "kind containingType signature filePath line"
          }
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "baseline"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_unused_references",
    "description": "Identifies NuGet package references in a project that are not being used.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": "string"
        }
      },
      "required": [
        "projectName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_interface_extraction_candidates",
    "description": "Finds public classes with 3+ public methods but no corresponding interface. Returns ClassName, FilePath, and the list of public method names. These are prime candidates for interface extraction — a prerequisite for testability (Moq/NSubstitute) and adding a second implementation. Increase minPublicMethods (default 3) for stricter filtering.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "minPublicMethods": {
          "type": "integer",
          "default": 3
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "generate_test_skeleton",
    "description": "Generates a unit test skeleton for a class.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "generate_classes_from_json",
    "description": "Generates C# classes from a JSON string.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "json": {
          "type": "string"
        },
        "rootClassName": {
          "type": "string"
        },
        "namespace": {
          "type": "string"
        }
      },
      "required": [
        "json",
        "rootClassName",
        "namespace"
      ]
    }
  },
  {
    "name": "analyze_control_flow",
    "description": "Analyzes control flow of a method: shows whether it always/sometimes/never returns, lists all return points, and identifies exit paths (throws, breaks, continues). methodName: the method to analyze. contextSnippet: optional snippet to disambiguate overloads. Provide lineBefore and/or lineAfter when the snippet could match multiple locations. Returns: AlwaysReturns, SometimesReturns, ReturnStatements, ThrowStatements, HasInfiniteLoop.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        },
        "contextSnippet": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "analyze_exception_handling",
    "description": "Analyzes exception handling anti-patterns in a file: CatchAll (bare catch or catch Exception), EmptyRethrow (throw ex; loses stack trace), SwallowedException (catch with no rethrow/log/return), ExceptionAsControlFlow (catching FormatException etc. inside loops). Severity: High for CatchAll/EmptyRethrow, Medium for SwallowedException/ExceptionAsControlFlow.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "record_to_class",
    "description": "Converts a C# record back to a standard class.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "recordName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "recordName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_public_api_surface_snapshot",
    "description": "Extracts a snapshot of the public API surface (all public/protected types, methods, constructors, properties, events) from a project or file. Save the returned list as a baseline, make code changes, then call detect_breaking_changes with the baseline to find removed or renamed members. Provide projectName to scope to one project, filePath to scope to one file, or omit both for the entire solution.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "introduce_parameter",
    "description": "Introduces a new method parameter from an expression. Provide contextSnippet: a verbatim substring uniquely identifying the expression. Provide lineBefore and/or lineAfter when the snippet could match multiple locations. Updates the method signature (single-file only — call sites in other files not updated).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "contextSnippet": {
          "type": "string"
        },
        "newParamName": {
          "type": "string"
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "contextSnippet",
        "newParamName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_use_frozen_collections",
    "description": "Finds private static readonly Dictionary<> and HashSet<> fields initialized inline. Suggests using FrozenDictionary/FrozenSet (System.Collections.Frozen) for better read performance.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "list_files",
    "description": "Lists all files within a specific project.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": "string"
        }
      },
      "required": [
        "projectName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "invert_assignments",
    "description": "Swaps left and right sides of all assignment statements within a line range.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "startLine": {
          "type": "integer"
        },
        "endLine": {
          "type": "integer"
        }
      },
      "required": [
        "filePath",
        "startLine",
        "endLine"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "generate_equality_overrides",
    "description": "Generates Equals and GetHashCode overrides for a class.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "optimize_task_wait",
    "description": "Adds .ConfigureAwait(false) to all await expressions in a file for library-safe async code.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "convert_anonymous_to_named",
    "description": "Converts an anonymous object creation to a formal named class.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "newClassName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "newClassName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "update_xml_docs_from_signature",
    "description": "Regenerates XML doc param/returns tags to match the current method signature: adds tags for new parameters, removes tags for deleted parameters, and adds a <returns> tag if missing on a non-void method.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "generate_default_config_json",
    "description": "Scans a project for all config[\"Key\"] and IConfiguration.GetValue<T>(\"Key\") usages and generates a JSON skeleton with all keys and inferred default values.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": "string"
        }
      },
      "required": [
        "projectName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "remove_member",
    "description": "Removes a specific member from a class or interface by name.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "memberName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "memberName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "use_index_from_end",
    "description": "IDE0056: Upgrades manual index calculations (Length - 1) to modern [^1] syntax.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_unsafe_lazy_init",
    "description": "Detects unsafe lazy initialization: double-checked locking without 'volatile' keyword (field may be partially initialized due to CPU reordering). Suggests Lazy<T> or volatile.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "move_file_to_namespace_folder",
    "description": "Returns the folder path where a file should reside based on its declared namespace. Use to plan file moves.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_solution_diagnostics",
    "description": "Gets compiler errors and warnings across the entire loaded solution.\r\nResults are capped at maxDetails entries in the detail list (default 50) to keep\r\noutput manageable — the Errors/Warnings counts always reflect the full totals.\r\nErrors are always sorted before warnings so the cap never hides an error.\r\nFile paths are relative to the solution root for compact output.\r\nFor complete diagnostics on a specific project, use get_project_diagnostics instead.\r\nBlazor source-generator false positives (CS0234/CS0246/CS0103) are automatically\r\nsuppressed in Razor-component projects.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "maxDetails": {
          "type": "integer",
          "default": 50
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "extract_constant",
    "description": "Extracts a literal expression to a named constant in the containing type. contextSnippet: a verbatim substring containing or surrounding the literal (e.g., '= 42;' or '\"hello world\"'). constantName: the name for the new constant. visibility: 'private' (default), 'protected', 'internal', or 'public'. Provide lineBefore and/or lineAfter when the snippet could match multiple locations. Returns the updated file content.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "contextSnippet": {
          "type": "string"
        },
        "constantName": {
          "type": "string"
        },
        "visibility": {
          "type": "string",
          "default": "private"
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "contextSnippet",
        "constantName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "document_poco_fields",
    "description": "Adds comprehensive [Description] comments to all fields in a POCO class.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "convert_switch_to_pattern_safe",
    "description": "Converts a switch statement to a switch expression safely.\n\nFIXES standard 'convert_to_pattern_matching' BUG: The standard tool silently\ndrops variable assignments for cases that set more than one variable, generating\nincorrect code. This tool rejects those cases with a clear error message instead\nof producing broken output.\n\nSupports three switch forms:\n  1. All cases are assignments to the SAME variable:\n       case \"g\": factor = 1.0; break;\n       → factor = unit switch { \"g\" => 1.0, ... };\n\n  2. All cases are return statements:\n       case \"g\": return 1.0;\n       → return unit switch { \"g\" => 1.0, ... };\n\n  3. All cases are throw statements (or mixed with return).\n\nRejects (with a clear error):\n  • Cases assigning to MULTIPLE different variables per case\n  • Cases assigning to different variables across cases\n  • Cases with complex multi-statement bodies\n\nRun AnalyzeSwitchForPatternConversion first if you are unsure.\n\nParameters:\n  filePath       - Absolute path to the .cs file.\n  contextSnippet - A verbatim substring from the switch keyword or governing\n                   expression, e.g. \"switch (unit)\".\n  lineBefore/lineAfter - Verbatim text from the line above/below the target to\n                   disambiguate when the snippet matches multiple locations.\n\nReturns: UpdatedContent on success, or Error describing exactly why conversion\nwas rejected.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "contextSnippet": {
          "type": "string"
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "contextSnippet"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_task_yield_usage",
    "description": "Detects Task.Yield() calls.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_public_api_surface",
    "description": "Returns the complete public API surface of a named project: all public types (classes, interfaces, records, structs), their public and protected methods, properties, and constructors. Each entry includes the type name, member name, full signature, kind (Class/Interface/Method/Property/Constructor/Record/Struct), virtuality/abstractness/sealed flags, and any XML documentation summary. Use includeMethods/includeProperties/includeTypes flags to filter output. Ideal for generating SDK documentation, comparing API surfaces across versions, or producing API review reports.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": "string"
        },
        "includeMethods": {
          "type": "boolean",
          "default": true
        },
        "includeProperties": {
          "type": "boolean",
          "default": true
        },
        "includeTypes": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "projectName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "sync_interface_to_implementation",
    "description": "Adds to the interface any public methods or properties that exist in the class but are missing from the interface. Finds the interface in the same file or anywhere in the solution. Returns the updated source of the file containing the interface (prefixed with '// Updated file: path' if it differs from the class file).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        },
        "interfaceName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className",
        "interfaceName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_naming_violations",
    "description": "Checks C# naming conventions across a file, project, or solution. Reports: private fields not following '_camelCase', non-private methods not following PascalCase, and parameters not following camelCase. Returns AntiPatternFinding records with Pattern='NamingViolation', Severity='Low'.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "generate_fluent_builder",
    "description": "Generates a fluent builder class for any C# class. The builder provides With{Property}() methods for each public settable property and a Build() method that constructs the target type. Returns the complete builder source, class name, and a usage example.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "validate_staged_changes",
    "description": "Validates a previously staged set of changes (from a refactoring tool) in-memory and returns compiler diagnostics. Use this for a 'dry run' before applying staged changes.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "changeId": {
          "type": "string"
        }
      },
      "required": [
        "changeId"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_configure_await_missing",
    "description": "Scans a file for await expressions missing .ConfigureAwait(false). Excludes classes ending in Controller/Hub/PageModel/ViewModel (those are app-level where ConfigureAwait is not needed). Returns method name, line, and reason.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "simplify_member_access",
    "description": "Removes redundant 'this.' member access qualifiers from an entire file.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_blocking_calls_in",
    "description": "Detects blocking calls inside async methods: Thread.Sleep, .Result, .Wait(), .GetAwaiter().GetResult(). These block the thread pool thread and can cause deadlocks.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_namespace_path_mismatches",
    "description": "Scans the loaded solution for source files where the declared namespace does not match the file's folder path relative to its project root. Mismatches indicate that Roslyn's path-based document identity and namespace-based type identity are out of sync, which can cause batch refactoring tools to write changes to incorrect file locations. Syntax-only scan — no compilation required. Safe to run at any time including when the build is broken. Returns findings grouped by severity: Error (duplicate type names exist across mismatched paths — immediate risk), Warning (mismatch exists but no duplicate detected — latent risk). Optionally scoped to a single project.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "add_braces",
    "description": "IDE0011: Adds braces to single-line if, foreach, and while statements.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "list_dependencies",
    "description": "Lists all project and NuGet dependencies for a specific project.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": "string"
        }
      },
      "required": [
        "projectName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_methods_by_return_type",
    "description": "Finds all methods in the solution that return a specific type.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "returnType": {
          "type": "string"
        }
      },
      "required": [
        "returnType"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_duplicate_methods",
    "description": "Finds structurally duplicate method implementations across the solution: methods that share the same statement structure and control flow even if identifiers differ (hash-based). Returns groups of duplicate methods with their file paths and type names. Use to find copy-paste code that should be consolidated into a shared helper. Increase minStatements (default 5) to reduce false positives on short utility methods.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "minStatements": {
          "type": "integer",
          "default": 5
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "extract_method",
    "description": "Extracts a block of statements into a new private method. Provide startLine and endLine (1-based, line range only — no column precision needed), the exact physical text of those two lines (used to detect stale files before modifying), and a name for the new method. Data flow analysis automatically determines parameters and return type. Returns BeforeSnippet/CallSiteReplacement/ExtractedMethodText previews plus UpdatedSourceContent for staging.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "startLine": {
          "type": "integer"
        },
        "startLineText": {
          "type": "string"
        },
        "endLine": {
          "type": "integer"
        },
        "endLineText": {
          "type": "string"
        },
        "newMethodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "startLine",
        "startLineText",
        "endLine",
        "endLineText",
        "newMethodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "remove_base_type",
    "description": "Removes a base type or interface from a type declaration.\n\ntypeName is the class/record/struct/interface to modify.\nbaseTypeName is the base type or interface to remove (e.g. \"IDisposable\", \"BaseController\").\nIf the type has no base list or does not implement/inherit baseTypeName, the file is returned unchanged.\nUse autoStage=true (default) to get a ChangeId for ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "typeName": {
          "type": "string"
        },
        "baseTypeName": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "typeName",
        "baseTypeName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_unvalidated_regex_source",
    "description": "Detects new Regex() or Regex.IsMatch/Match/Replace calls where the pattern argument is not a compile-time string literal. A non-literal pattern may originate from user input, enabling Regex injection (attacker-controlled matching logic) and ReDoS attacks.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_circular_type_references",
    "description": "Finds circular constructor-injection dependencies among user-defined types: type A's constructor depends on type B, and B's constructor (transitively) depends on A. This is the exact cycle that causes .NET's DI container to throw at startup. Complements find_services_not_registered — use both before adding new service registrations. Optionally scoped to a project.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "add_field",
    "description": "Generates a field declaration and adds it to a type.\n\ncontainerName is the class/struct to modify; fieldName and fieldType specify the field.\naccessibility defaults to \"private\". isReadonly and isStatic add those modifiers.\ninitializer is an optional value expression (e.g. \"42\" or \"new List<string>()\").\nUse autoStage=true (default) to get a ChangeId for ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "containerName": {
          "type": "string"
        },
        "fieldName": {
          "type": "string"
        },
        "fieldType": {
          "type": "string"
        },
        "accessibility": {
          "type": "string",
          "default": "private"
        },
        "isReadonly": {
          "type": "boolean",
          "default": false
        },
        "isStatic": {
          "type": "boolean",
          "default": false
        },
        "initializer": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "containerName",
        "fieldName",
        "fieldType"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_possible_infinite_loops",
    "description": "Analyzes a file for potential infinite loops.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "check_for_sql_injection",
    "description": "Scans for potential SQL injection vulnerabilities.\r\nPass filePath to scope to a single file, projectName to scope to a project,\r\nor leave both null to scan the whole solution.\r\n\r\nDetects calls to common SQL execution methods (ExecuteNonQuery, ExecuteReader,\r\nExecuteScalar, FromSqlRaw, Query, etc.) where the first argument is a dynamic\r\nstring — either an interpolated string with expressions ($\"...{x}...\") or string\r\nconcatenation involving a non-literal operand.\r\n\r\nReturns a list of SecurityIssueReport with IssueType='PossibleSqlInjection',\r\nfile path, line/column, and a description recommending parameterized queries.\r\nDoes NOT check CommandText property assignments.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "replace_constructor_with_factory",
    "description": "Replaces a class constructor with a private constructor plus a public static Create() factory method.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_symbol_info",
    "description": "Gets deep metadata for a symbol: type, kind, accessibility, attributes, documentation. Provide contextSnippet: a verbatim substring identifying the symbol usage or declaration. Provide lineBefore and/or lineAfter when the snippet could match multiple locations.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "contextSnippet": {
          "type": "string"
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "contextSnippet"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "optimize_independent_awaits",
    "description": "Batches independent sequential await calls in a method into a single Task.WhenAll() for parallel execution.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_unobserved_task_in_field",
    "description": "Detects Task or ValueTask-returning method calls assigned to fields or properties without await, where the field is never subsequently awaited or .Wait()ed anywhere in the class. The task silently fails — any exception it throws is never observed and may eventually crash the process via UnobservedTaskException.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_implementations_safe",
    "description": "Finds all implementations of an interface member or virtual/abstract method in the solution\nwithout requiring line/column coordinates.\nUnlike the built-in find_implementations which needs a line number, this locates the symbol by name\nwith an optional contextSnippet.\nsymbolName: the interface member or virtual method name.\ncontextSnippet: optional verbatim substring of the declaration to disambiguate overloads.\nProvide lineBefore and/or lineAfter when the snippet could match multiple locations.\nReturns TypeName, FilePath, Line, and Kind for each implementing symbol.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "symbolName": {
          "type": "string"
        },
        "contextSnippet": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "symbolName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "safe_delete",
    "description": "Safely deletes a symbol (method, property, class) only if it has zero usages in the entire codebase.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "line": {
          "type": "integer"
        },
        "column": {
          "type": "integer"
        }
      },
      "required": [
        "filePath",
        "line",
        "column"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_todo_fixme_comments",
    "description": "Scans for TODO, FIXME, HACK, REVIEW, NOTE, BUG comments (case-insensitive) in a file, project, or entire solution. Sorted by severity: BUG/FIXME > HACK > TODO > REVIEW > NOTE.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "inline_parameter",
    "description": "Removes a method parameter that is always passed the same compile-time constant literal (e.g. 42, true, \"text\") at every call site, and replaces references to the parameter inside the method body with that constant. Only works when ALL call sites pass the same constant — rejects if call sites are in other files, if the argument varies, or if the argument is not a literal.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        },
        "parameterName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName",
        "parameterName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_readonly_field_candidates",
    "description": "Finds private, non-readonly fields that are only ever assigned inside constructors and could safely be marked readonly.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_hardcoded_paths",
    "description": "Scans for hardcoded file system paths. Pass filePath to scope to a single file, projectName to scope to a project, or leave both null to scan the whole solution.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "verify_interface_completeness",
    "description": "Checks every class that implements an interface and reports which interface members each implementor has covered. Useful for finding partially-implemented interfaces across the solution.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "interfaceName": {
          "type": "string"
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "interfaceName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "convert_abstract_to_interface",
    "description": "Converts an abstract class to an interface.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_unbounded_recursion",
    "description": "Detects recursive methods that lack a depth parameter or an early-exit base-case guard (non-recursive if-return before the recursive call). Unbounded recursion on large inputs causes StackOverflowException and crashes the process with no recoverable exception.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_sequential_independent_awaits",
    "description": "Detects consecutive await expressions in async methods where neither result is used by the other await — missed Task.WhenAll() parallelism. Example: var a = await F(); var b = await G(); where b doesn't depend on a. Combining with Task.WhenAll cuts total latency to max(F,G).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "run_bridge_batch",
    "description": "Converts a batch of methods flagged with [MigrationCandidate(\"AsyncBridgeCandidate\")] to\r\nthe Asyncify-bridge pattern in a single call, using in-memory Roslyn compilation for\r\nvalidation (no MSBuild round-trips). For each candidate:\r\n  1. Calls convert_to_async_bridge to produce the transformed source.\r\n  2. If propagateCancellationTokens=true: propagates CT to async callees in the new Async overload.\r\n  3. Validates the composite source with in-memory compilation.\r\n  4. On success: writes the file to disk and refreshes the workspace.\r\n  5. On error: flags the method [MigrationCandidate(\"NeedsManualReview\")] and continues.\r\n\r\nParameters:\r\n  projectName                 — restrict candidate scan to one project (null = entire solution).\r\n  maxBridges                  — max methods to process this call (default 10).\r\n  scoreThreshold              — only candidates with score ≤ this are eligible (default 60).\r\n                                flag_migration_candidates_in_project uses minScore 50, so no\r\n                                candidate is flagged below 50. 60 captures the simplest batch first.\r\n  dryRun                      — when true, reports what would happen without writing files.\r\n  propagateCancellationTokens — when true (default), also propagates the CT parameter to\r\n                                other async callees in the new async overload.\r\n\r\nReturns BridgeBatchResult with:\r\n  Applied             — methods successfully bridged and written to disk.\r\n  Skipped             — methods that failed (each flagged NeedsManualReview).\r\n  RemainingCandidates — count of eligible [MigrationCandidate] methods still pending.\r\n  StopReason          — \"batch_complete\" | \"budget_exhausted\" | \"no_candidates\" | \"dry_run\".",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "maxBridges": {
          "type": "integer",
          "default": 10
        },
        "scoreThreshold": {
          "type": "integer",
          "default": 60
        },
        "dryRun": {
          "type": "boolean",
          "default": false
        },
        "propagateCancellationTokens": {
          "type": "boolean",
          "default": true
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "create_project",
    "description": "Creates a new project and adds it to the current solution.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": "string"
        },
        "projectType": {
          "type": "string",
          "default": "console"
        }
      },
      "required": [
        "projectName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "apply_proposed_changes",
    "description": "Commits a dictionary of file paths and their new contents to disk. Supports automatic retry for transient file locks.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "changes": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "retryCount": {
          "type": "integer",
          "default": 3
        }
      },
      "required": [
        "changes"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "convert_lock_to_semaphore_slim",
    "description": "Converts lock statements inside a method to async-safe SemaphoreSlim pattern: adds a 'private readonly SemaphoreSlim _semaphore = new(1,1)' field, replaces each lock block with 'await _semaphore.WaitAsync(); try { ... } finally { _semaphore.Release(); }', and makes the method async if needed. Returns the updated source as a string. Does NOT write to disk or update the workspace. Pass the result to apply_proposed_changes to save.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "detect_long_parameter_lists",
    "description": "Detects methods with too many parameters and suggests a Parameter Object, optionally filtered by project.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "threshold": {
          "type": "integer",
          "default": 5
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "modernize_exceptions",
    "description": "Replaces generic throw new Exception calls with custom typed exceptions and generates the new class files.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "targets": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": "@{filePath=; line=; newExceptionName=}",
            "required": "filePath line newExceptionName"
          }
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "targets"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "add_guard_clauses",
    "description": "Adds ArgumentNullException.ThrowIfNull guard clauses for all reference parameters in a method. Returns the updated source as a string. Does NOT write to disk or update the workspace. Pass the result to apply_proposed_changes to save.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_migration_candidates",
    "description": "Returns all methods in the solution (or scoped to a file/project) that carry a\r\n[MigrationCandidate] attribute added by flag_migration_candidate.\r\n\r\nfilePath:    restrict to a single file (full or partial path suffix).\r\nprojectName: restrict to a single project (case-insensitive name).\r\npattern:     restrict to one pattern — \"AsyncBridgeCandidate\", \"HandlerExtract\",\r\n             \"HandlerToAsync\", \"AsyncCallerUplift\", etc. Omit to return all patterns.\r\n\r\nUses syntax-level analysis — no compilation needed — so results are accurate even\r\nimmediately after flag_migration_candidate without a solution reload.\r\n\r\nReturns one MigrationCandidateFinding per flagged method per attribute.\r\nEach finding includes a Summary field for human-readable log output.\r\nA method flagged for two different patterns appears twice.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "pattern": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "extract_interface",
    "description": "Extracts an interface from a class: creates a new file '{interfaceName}.cs' containing all public non-static properties and methods, then adds the interface to the class's base list. Returns a staged ChangeId (autoStage=true, default) or the raw file dictionary. Static members and constructors are excluded.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        },
        "interfaceName": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "className",
        "interfaceName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "class_to_record",
    "description": "Converts a class to a C# record.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_non_exhaustive_enum_switches",
    "description": "Finds switch statements on enum types that don't handle all enum members and have no default case. Returns the enum type name, the list of missing member names, the containing method name, file path, and line. Essential after adding a new enum member — tells you every switch that needs updating. Scope to a file or project, or scan the entire solution.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_concurrent_collection_opportunities",
    "description": "Finds lock statements that protect List<T> or Dictionary<K,V> fields and suggests replacing them with ConcurrentDictionary<K,V> or ImmutableDictionary for better performance.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "convert_to_null_coalescing",
    "description": "Modernizes null checks using null coalescing operators (?? and ??=).\nConverts patterns like:\n- 'if (x == null) x = y;' → 'x ??= y;'\n- 'x == null ? y : x' → 'x ?? y'\n- 'x != null ? x : defaultValue' → 'x ?? defaultValue'\nfilePath: path to the .cs file to transform.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_cas_loop_without_backoff",
    "description": "Detects while/do-while loops containing Interlocked.CompareExchange that have no back-off (no Thread.Sleep, Thread.SpinWait, SpinWait.SpinOnce, or Task.Delay). A spinning CAS loop without back-off can peg a CPU core at 100% under contention (live-lock).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "move_all_types_to_files",
    "description": "Atomically moves all secondary types in a file to their own files. Returns a ChangeId for ApplyStagedChanges plus first-15-line previews of each affected file's new content.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_best_insertion_point",
    "description": "Returns the best 1-based line number where a new member of the given kind should be inserted in a type, following standard C# ordering (fields → constructors → destructors → properties → events → methods → nested types).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "containerName": {
          "type": "string"
        },
        "memberKind": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "containerName",
        "memberKind"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "detect_reflection_usage",
    "description": "Scans for System.Reflection usage. Optionally filtered by project.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "append_completed_work",
    "description": "Appends an entry to a completed-work log in docs/completed/. Append-only — existing content cannot be overwritten or deleted. Use to record finished work as an immutable audit trail. Only bare filenames and documentation extensions are accepted. Entry size capped at 512 KB.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filename": {
          "type": "string"
        },
        "entry": {
          "type": "string"
        }
      },
      "required": [
        "filename",
        "entry"
      ]
    }
  },
  {
    "name": "make_class_immutable",
    "description": "Makes a class immutable: adds 'readonly' to all fields and converts property setters to 'init'.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "move_type_to_file",
    "description": "Moves a type to its own file. Returns a ChangeId for ApplyStagedChanges plus a ContentPreviews map showing first 10 + last 10 lines of each affected file for structural confirmation. Use autoStage=false to get the full raw file content dictionary instead.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "typeName": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "typeName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "generate_to_string",
    "description": "Generates a public override ToString() for a class based on its public properties.\n\nSECURITY: Sensitive properties are automatically excluded from the output to prevent\ncredential leakage in logs. Properties whose names contain any of these substrings\n(case-insensitive) are excluded by default: password, passwd, secret, apikey, token,\nhash, salt, pin, cvv, ssn, creditcard, connectionstring, privatekey, clientsecret.\n\nUse excludeProperties to additionally exclude specific property names by exact name.\n\nReturns: UpdatedContent (the new file source), IncludedProperties, ExcludedProperties,\nand an optional Warning listing which properties were excluded and why.\nSkips generation if a ToString() override already exists.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        },
        "excludeProperties": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string null"
          },
          "default": null
        }
      },
      "required": [
        "filePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "generate_path_driven_tests",
    "description": "Generates path-driven xUnit/NUnit/MSTest test stubs for a method by walking\r\nits AST for decision points (if/else, switch, foreach) and emitting one test\r\ncase per distinct execution path. Always includes a happy-path test first.\r\n\r\nFor each path the report includes:\r\n- TestMethodName: suggested test method name following the MethodName_Condition_Outcome pattern\r\n- ScenarioDescription: human-readable description of the path\r\n- InputConstraints: inferred parameter values to trigger this path\r\n- ArrangeCode / ActCode / AssertCode: Arrange/Act/Assert scaffold with TODO markers\r\n- Note: caveats about heuristic inference\r\n\r\nGenerated code is a starting point — TODO markers show where you must supply\r\nreal values or mock setups that cannot be inferred statically.\r\n\r\nframework: \"NUnit\" (default), \"xunit\", or \"mstest\"\r\ndisambiguateLine: any line number inside the desired overload when the method is overloaded",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        },
        "framework": {
          "type": "string",
          "default": "NUnit"
        },
        "disambiguateLine": {
          "type": [
            "integer",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "add_validation_to_poco",
    "description": "Adds [Required] and [StringLength(100)] data annotations to all string properties in a POCO class.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "apply_staged_changes",
    "description": "Commits a previously staged set of changes (from a refactoring tool) to disk.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "changeId": {
          "type": "string"
        },
        "retryCount": {
          "type": "integer",
          "default": 3
        }
      },
      "required": [
        "changeId"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "inline_method",
    "description": "Inlines a method solution-wide: replaces ALL call sites (across all files in the solution) with the method's expression, then removes the method declaration. Supported: expression-body methods (=> expr) and single-return-statement methods. Methods with multiple statements are rejected. Returns a dictionary of filePath→updatedContent for every file that was modified.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "encapsulate_field_safe",
    "description": "Encapsulates a public field into a private backing field + public property.\n\nFIXES standard 'encapsulate_field' BUG: The standard tool generates code like\n  private int SuccessCount;\n  public int SuccessCount { get { return SuccessCount; } }\nwhich causes infinite recursion / a compile error because the backing field and\nproperty share the same name.\n\nThis tool always renames the backing field to _camelCase:\n  private int _successCount;\n  public int SuccessCount { get => _successCount; set => _successCount = value; }\n\nParameters:\n  filePath            - Absolute path to the .cs file.\n  fieldName           - Exact name of the field to encapsulate (e.g. \"SuccessCount\").\n  overridePropertyName - Optional: provide a custom property name if the default\n                         (PascalCase of fieldName) would conflict.\n\nReturns: UpdatedContent with the corrected encapsulation applied.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "fieldName": {
          "type": "string"
        },
        "overridePropertyName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "fieldName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_double_checked_locking",
    "description": "Detects the double-checked locking (DCL) pattern where the lazily-initialized field is not declared volatile. Without volatile, a CPU or JIT may reorder the store and expose a partially-constructed object to the outer null-check on another thread. Use volatile or Lazy<T>.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "interpolate_string_safe",
    "description": "Converts a string.Format(...) call to an interpolated string ($\"...\").\nUnlike the built-in convert_to_interpolated_string, this resolves const string format arguments\nvia the semantic model, so it works even when the format string is a named const rather than a\nliteral. Handles {0:format} format specifiers correctly.\ncontextSnippet: verbatim substring identifying the string.Format call to convert (required).\nProvide lineBefore and/or lineAfter when the snippet could match multiple locations.\nReturns the updated file content.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "contextSnippet": {
          "type": "string"
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "contextSnippet"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "diagnose",
    "description": "Checks the health of the Roslyn MCP server environment and workspace status.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "solutionPath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "verbose": {
          "type": "boolean",
          "default": false
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_di_registrations",
    "description": "Scans for all DI registrations (AddSingleton/AddScoped/AddTransient) across the solution or in a specific project/file. Returns service type, implementation type, lifetime, and source location for each registration. Use lifetimeFilter to narrow results ('Singleton', 'Scoped', 'Transient').",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lifetimeFilter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "upgrade_to_file_scoped_namespace",
    "description": "IDE0210: Converts a block-form namespace declaration ('namespace Foo { }') to C# 10 file-scoped form ('namespace Foo;'). Preserves using directives, extern aliases, and all type members. Returns the rewritten file content.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "read_current_state",
    "description": "Reads the migration state file (docs/migration-state.yaml). Takes no parameters — there is exactly one state file.",
    "inputSchema": {
      "type": "object",
      "properties": {}
    }
  },
  {
    "name": "move_all_types_to_files_in_solution",
    "description": "Atomically moves all secondary types in every file across the entire solution to their own files. If autoStage is true, returns a ChangeId.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "implement_interface_safe",
    "description": "Generates stub implementations for all unimplemented members of an interface on a class. Unlike the built-in implement_interface, this never adds the 'override' keyword (which is incorrect for interface implementations). Pass filePath of the class file, className of the implementing class, and interfaceName of the interface to implement.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        },
        "interfaceName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className",
        "interfaceName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "use_field_backed_properties",
    "description": "C# 14: Upgrades manual backing fields to modern 'field' keyword auto-properties.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "generate_call_tree",
    "description": "Generates a markdown call tree for a specific method.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        },
        "depth": {
          "type": "integer",
          "default": 3
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "convert_out_params_to_value_tuple",
    "description": "Converts a method that uses 2+ 'out' parameters to return a ValueTuple instead.\nRewrites the method declaration (return type, parameter list, return statements)\nand all call sites in the solution.\n\nTransformation applied:\n  void M(T1 a, out T2 b, out T3 c)  →  (T2 b, T3 c) M(T1 a)\n  bool M(T1 a, out T2 b, out T3 c)  →  (bool result, T2 b, T3 c) M(T1 a)\n\nCall site rewrites:\n  M(arg, out x, out y)          →  var (x, y) = M(arg);\n  bool ok = M(arg, out x, out y) →  var (ok, x, y) = M(arg);\n\nComplex call sites (e.g. inline in conditional expressions) receive a TODO\ncomment and are listed in CallSiteWarnings for manual review.\n\nUse FindMultipleOutParameterMethods first to identify candidates.\nChanges are applied directly to the workspace via Roslyn's DocumentEditor.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "analyze_path_coverage",
    "description": "Analyzes execution paths for test coverage.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "apply_proposed_diff",
    "description": "Applies a Unified Diff to a file and writes the result to disk. Returns ApplyChangesResult with SucceededFiles, WorkspaceInSync, and WorkspaceVersion. To preview the result without writing, use validate_proposed_diff instead.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "unifiedDiff": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "unifiedDiff"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "introduce_parameter_object",
    "description": "Encapsulates method parameters into a new C# 12 record type. Groups all non-CancellationToken parameters (or only those specified in parameterNames) into a 'public record {NewTypeName}(...)'. Rewrites parameter references in the method body to 'request.PropertyName'. Appends the record to the end of the file. Adds a TODO comment in the method body reminding to update call sites.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        },
        "newTypeName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "parameterNames": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string null"
          },
          "default": null
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_async_over_sync",
    "description": "Finds async methods with no await expressions, or that only await Task.FromResult/Task.CompletedTask.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_extension_methods",
    "description": "Finds all extension methods whose receiver type matches the given type (or its base types / interfaces). Returns method name, full signature, defining class, namespace, file path, and line.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "targetTypeName": {
          "type": "string"
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "targetTypeName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_primitive_obsession",
    "description": "Finds methods/constructors where the same primitive type (string/int/long/Guid/bool) appears 3+ times as distinct parameters.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "analyze_semaphore_usage",
    "description": "Analyzes SemaphoreSlim usage to find potentially missing Release() calls.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "introduce_field",
    "description": "Introduces a new private readonly field from an expression. Provide contextSnippet: a verbatim substring from the expression (e.g., the expression text itself or the surrounding assignment). Provide lineBefore and/or lineAfter when the snippet could match multiple locations. The expression type is inferred from semantics.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "contextSnippet": {
          "type": "string"
        },
        "newFieldName": {
          "type": "string"
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "contextSnippet",
        "newFieldName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "add_modifier",
    "description": "Adds a modifier keyword to a type or member (idempotent).\n\ntargetName is the class/method/property name to modify.\nmodifier is the keyword to add: virtual, abstract, sealed, static, readonly, override,\npartial, async, new, extern, unsafe, volatile.\nUse autoStage=true (default) to get a ChangeId for ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "targetName": {
          "type": "string"
        },
        "modifier": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "targetName",
        "modifier"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "list_projects",
    "description": "Lists all projects in the current solution.",
    "inputSchema": {
      "type": "object",
      "properties": {}
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_missing_generic_constraints",
    "description": "Finds generic methods with type parameters used in ways that require constraints but have none declared: (1) 'new T()' instantiation without 'where T : new()', (2) 'param == null' comparison without 'where T : class' (always false for value types). Returns file path, line number, method name, and a description of the specific violation. Scope by file or project, or scan the entire solution.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "analyze_performance",
    "description": "Analyzes a file for common performance issues.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "detect_unused_local_variables",
    "description": "Identifies local variables that are declared but never used within their scope.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_type_hierarchy",
    "description": "Returns the full type hierarchy for a named type: direct base class, full base class chain\n(excluding System.Object), all implemented interfaces (including transitive ones), derived\nclasses (via SymbolFinder.FindDerivedClassesAsync), and — if the type is an interface —\nall implementing types (via FindImplementationsAsync).\nEach entry includes TypeName, FilePath, Line, and Kind (Class/Interface/Struct).\nIsInterface, IsAbstract, IsSealed flags are included on the root type.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "typeName": {
          "type": "string"
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "typeName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_test_coverage_map",
    "description": "Extends path coverage analysis with a cross-reference to test methods that exercise the given production method. Finds covering tests by name convention (test method name contains the production method name) and by direct call-site presence in the test body. Returns BranchesToTest (execution paths to cover) and CoveringTests (test file, test method name, line) with HasAnyCoverage flag.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "generate_xml_documentation_stubs",
    "description": "Generates XML documentation stubs (<summary>, <param>, <returns>) for ALL undocumented public methods in a file. Unlike document_poco_fields (which targets a specific class's fields), this covers every public method in the file that lacks XML docs. Returns the updated file content. Apply with apply_proposed_changes to write to disk.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_project_diagnostics",
    "description": "Gets all compiler errors and warnings for a specific project.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": "string"
        }
      },
      "required": [
        "projectName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "add_constructor_parameter",
    "description": "Adds a DI constructor parameter in one step: private readonly field + parameter + body assignment.\n\nclassName is the target class; paramName and paramType define the parameter.\nfieldName overrides the derived field name (defaults to _camelCase of paramName).\nIf the class has no constructor one is created. Expression-bodied constructors are converted to block bodies.\nUse autoStage=true (default) to get a ChangeId for ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        },
        "paramName": {
          "type": "string"
        },
        "paramType": {
          "type": "string"
        },
        "fieldName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "className",
        "paramName",
        "paramType"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_unsafe_lazy_init_thread",
    "description": "Detects the unsafe lazy initialization pattern (if (_field == null) { _field = new X(); }) outside a lock and without a volatile field or Lazy<T>. Without volatile, the CPU or JIT may reorder the store and expose a partially-constructed object on another thread.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "update_plan",
    "description": "Writes (creates or overwrites) a plan file in the solution's docs/plans/ directory. Only bare filenames and documentation extensions are accepted. Content size capped at 512 KB.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filename": {
          "type": "string"
        },
        "content": {
          "type": "string"
        }
      },
      "required": [
        "filename",
        "content"
      ]
    }
  },
  {
    "name": "discard_staged_changes",
    "description": "Manually removes a staged change set from the server-side buffer without applying it.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "changeId": {
          "type": "string"
        }
      },
      "required": [
        "changeId"
      ]
    }
  },
  {
    "name": "flag_migration_candidates_in_project",
    "description": "Autonomously scans every method in a project (or the whole solution), scores each\r\nagainst the specified migration pattern, and stamps qualifying methods with\r\n[MigrationCandidate(\"pattern\")] — all in a single call. No agent iteration over\r\nfiles or methods required.\r\n\r\nprojectName: name of the project to scan (case-insensitive). Omit to scan the whole solution.\r\npattern:     migration pattern to apply. Default \"AsyncBridgeCandidate\".\r\n             Known patterns: AsyncBridgeCandidate, HandlerExtract, HandlerToAsync, AsyncCallerUplift.\r\nminScore:    minimum score a method must reach to be flagged. Default 50.\r\ndryRun:      when true, scores methods and returns what WOULD be flagged without writing any\r\n             files. Use to preview before committing.\r\nforceRescan: when true, ignores existing [MigrationCandidate] attributes and re-evaluates\r\n             every method from scratch. Use after changing scoring rules for a clean result.\r\n\r\nScoring heuristics (AsyncBridgeCandidate):\r\n  +40 — body contains blocking calls (.GetAwaiter().GetResult(), .Result, .Wait())\r\n  +30 — body calls CommonSearch (the project's async-ready SQL wrapper)\r\n  +25 — body uses SqlCommand / SqlConnection / getDataContext / DataContext\r\n  +15 — class name ends with Service\r\n  +5  — method is static (easier to bridge in isolation)\r\n  -20 — method is virtual or override (interface widening may be required)\r\n  disqualified — abstract, extern, already async, name ends with Async,\r\n                 has yield return, has ref/out parameters, carries [Obsolete],\r\n                 has sql-access only with no async leaf (linq-to-sql-only)\r\nEvent handlers (object sender, XxxEventArgs e) are automatically tagged as\r\nAsyncHandlerCandidate instead of AsyncBridgeCandidate, with a +20 semantic bonus when\r\nthe handler calls an [Obsolete]-decorated bridge wrapper.\r\nThe +20 calls-obsolete-wrapper bonus applies to ALL methods (not just handlers) that\r\ncall any [Obsolete]-decorated method — catching helpers like initComboBox() that call\r\nthe deprecated sync CommonSearch.search() and need to migrate to searchAsync().\r\n\r\nReturns FlagCandidatesInProjectResult:\r\n  Flagged        — methods written (or that would be written when dryRun=true)\r\n  Skipped        — methods scored but below minScore\r\n  AlreadyFlagged — methods that already had [MigrationCandidate(\"pattern\")]\r\n  TotalMethodsExamined / TotalFilesWritten for progress tracking",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "pattern": {
          "type": "string",
          "default": "AsyncBridgeCandidate"
        },
        "minScore": {
          "type": "integer",
          "default": 50
        },
        "dryRun": {
          "type": "boolean",
          "default": false
        },
        "forceRescan": {
          "type": "boolean",
          "default": false
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "remove_attribute",
    "description": "Removes a named attribute from a type or member.\n\ntargetName is the class/method/property name to modify.\nattributeName is the attribute to remove, with or without the 'Attribute' suffix and brackets\n(e.g. \"Obsolete\", \"ObsoleteAttribute\", or \"[Obsolete]\" — all match [Obsolete]).\nUse autoStage=true (default) to get a ChangeId for ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "targetName": {
          "type": "string"
        },
        "attributeName": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "targetName",
        "attributeName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_re_do_s_patterns",
    "description": "Detects Regex patterns in string literals that contain nested quantifiers ((a+)+, (a*b*)+, etc.) — a common ReDoS vulnerability. On non-matching input these patterns cause catastrophic backtracking, making the CPU spin indefinitely. Reports the pattern and the dangerous construct.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_finalizer_on_disposable",
    "description": "Detects classes that implement IDisposable AND declare a finalizer (~C()) without a disposed-flag guard (if (_disposed) return;). The GC may call the finalizer after Dispose() has already run, causing double-free of unmanaged resources.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "acknowledge_sync",
    "description": "Clears the external drift list, indicating the AI has acknowledged and read the latest disk changes.",
    "inputSchema": {
      "type": "object",
      "properties": {}
    }
  },
  {
    "name": "use_exception_expressions",
    "description": "Replaces throw new ArgumentNullException(nameof(x)) with ArgumentNullException.ThrowIfNull(x) and throw new ArgumentOutOfRangeException(nameof(x)) with ArgumentOutOfRangeException.ThrowIfNegative(x) in the specified method.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "read_completed_work",
    "description": "Reads a completed-work log file from the solution's docs/completed/ directory. Only bare filenames and documentation extensions are accepted.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filename": {
          "type": "string"
        }
      },
      "required": [
        "filename"
      ]
    }
  },
  {
    "name": "find_unused_constructors",
    "description": "Identifies constructors that are never called in the entire solution.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "upgrade_to_primary_constructor",
    "description": "Converts a class with a simple assignment-only constructor into a C# 12 primary constructor. Removes the explicit constructor and its corresponding private readonly fields, then rewrites field references to use the parameter names directly. Only succeeds if every constructor statement is a field assignment.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "read_project_documentation",
    "description": "Reads a project documentation file from the solution's docs/documentation/ directory. The filename parameter is treated as a bare filename — any path components are stripped and rejected. Only documentation file types (.md, .yaml, .yml, .json, .txt) are permitted. Cannot read source files or files outside the docs directory.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filename": {
          "type": "string"
        }
      },
      "required": [
        "filename"
      ]
    }
  },
  {
    "name": "find_string_format_in_loops",
    "description": "Detects string interpolation ($\"...\") and string.Format() calls inside loop bodies. Each iteration allocates a new string — use StringBuilder for accumulation or move the format outside the loop.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "pull_up_member",
    "description": "Pulls a member (method or property) up from a derived class to its base class.\n\nRemoves the 'override' modifier and adds 'virtual' (if not already abstract/virtual),\nthen moves the declaration from the derived class into the base class. Returns a two-file\nchange dict: derived class file (with member removed) + base class file (with member added).\n\nRequires: the source class has a base class with accessible source code in the solution.\nIf autoStage is true (default), returns a ChangeId for use with ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        },
        "memberName": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "className",
        "memberName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "extract_class",
    "description": "Extracts named members (methods, properties, fields) from a class into a new '{newClassName}.cs' file. The source class is updated: extracted members are removed and a public auto-property 'public {NewClassName} {NewClassName} { get; } = new();' is added as the composition point, making the extracted class accessible to external callers. Cross-file call sites that previously called 'sourceObj.Method()' are automatically rewritten to 'sourceObj.NewClassName.Method()' solution-wide. Returns a dictionary of filePath→updatedContent for every affected file.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        },
        "newClassName": {
          "type": "string"
        },
        "memberNames": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "filePath",
        "className",
        "newClassName",
        "memberNames"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "generate_async_overload",
    "description": "Generates an async overload for a synchronous method by wrapping it in Task.Run.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "move_all_types_to_files_in_project",
    "description": "Atomically moves all secondary types in every file in a project to their own files. If autoStage is true, returns a ChangeId.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "projectName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "propagate_cancellation_token_batch",
    "description": "Batch propagation of CancellationToken across multiple files. Accepts a list of file\r\npaths with optional per-file method filters. Processes each file independently,\r\nvalidating each in-memory before writing. Failed files do not block successful ones.\r\nOn failure with FlagFailures=true, flags offending methods with\r\n[MigrationCandidate(\"CancellationTokenForwardCandidate\")] for later manual review.\r\n\r\nInput (PropagateCtBatchInput):\r\n  Targets      — list of { FilePath, MethodNames? } targets.\r\n  DryRun       — when true, computes results without writing files (default false).\r\n  MaxFiles     — max files to process (default 100).\r\n  FlagFailures — when true (default), flags failing methods with MigrationCandidate.\r\n\r\nReturns PropagateCtBatchResult with:\r\n  Applied         — per-file results for files that were successfully processed.\r\n  Failed          — files that failed validation, with diagnostics and flagged methods.\r\n  TotalForwarded  — total call sites rewritten across all files.\r\n  TotalSkipped    — total call sites skipped.\r\n  RemainingFiles  — number of targets not processed (due to MaxFiles cap).\r\n  StopReason      — \"batch_complete\" | \"budget_exhausted\" | \"dry_run\".",
    "inputSchema": {
      "type": "object",
      "properties": {
        "input": {
          "type": "object",
          "properties": {
            "targets": "@{type=array; items=}",
            "dryRun": "@{type=boolean}",
            "maxFiles": "@{type=integer}",
            "flagFailures": "@{type=boolean}"
          }
        }
      },
      "required": [
        "input"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "wrap_in_region",
    "description": "Wraps a line range with #region / #endregion preprocessor directives.\n\nstartLine and endLine are 1-based line numbers. regionName is the label after #region.\nThis uses text manipulation rather than AST transformation.\nUse autoStage=true (default) to get a ChangeId for ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "startLine": {
          "type": "integer"
        },
        "endLine": {
          "type": "integer"
        },
        "regionName": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "startLine",
        "endLine",
        "regionName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "validate_proposed_changes",
    "description": "Validates a dictionary of proposed file changes in-memory and returns compiler diagnostics. Use this for a 'dry run' before applying changes.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "changes": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        }
      },
      "required": [
        "changes"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "detect_memory_leaks",
    "description": "Scans a file for potential memory leaks (e.g. unhooked events).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "retry_failed_changes",
    "description": "Retries committing previously failed file changes using server-side cached content. Token-efficient: no need to re-send file contents.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "specificFiles": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string null"
          },
          "default": null
        },
        "retryCount": {
          "type": "integer",
          "default": 3
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "detect_inefficient_string_comparisons",
    "description": "Scans for common string comparison pitfalls. Optionally filtered by project.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "reduce_block_depth",
    "description": "Inverts a single-branch if statement to an early-return guard clause, reducing nesting depth.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "check_for_redundant_cast",
    "description": "Detects redundant type casts. Optionally filtered by project.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "update_current_state",
    "description": "Writes the migration state file (docs/migration-state.yaml). Takes no filename parameter — there is exactly one state file. Content size capped at 512 KB.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "content": {
          "type": "string"
        }
      },
      "required": [
        "content"
      ]
    }
  },
  {
    "name": "get_external_changes",
    "description": "Returns a list of files that have been modified externally (on disk) since the AI last synced.",
    "inputSchema": {
      "type": "object",
      "properties": {}
    }
  },
  {
    "name": "analyze_data_flow",
    "description": "Analyzes data flow of a method body: identifies variables read before assignment, variables that flow out, write-only variables (possible bugs), and captured closure variables. methodName: the method to analyze. contextSnippet: optional snippet to disambiguate overloads. Provide lineBefore and/or lineAfter when the snippet could match multiple locations. Returns: ReadBeforeAssignment, WrittenInside, ReadInside, CapturedVariables, DataFlowWarnings.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        },
        "contextSnippet": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_unawaited_fire_and_forget",
    "description": "Finds Task-returning methods called without await (fire-and-forget). Exceptions will be silently swallowed. Pass filePath to scope to a single file, projectName to scope to a project, or leave both null to scan the whole solution.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "write_handoff",
    "description": "Writes (creates or overwrites) a session handoff document in the solution's docs/handoffs/ directory. Only bare filenames and documentation extensions are accepted. Content size capped at 512 KB.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filename": {
          "type": "string"
        },
        "content": {
          "type": "string"
        }
      },
      "required": [
        "filename",
        "content"
      ]
    }
  },
  {
    "name": "add_property",
    "description": "Generates an auto-property and adds it to a type.\n\ncontainerName is the class/record/struct to modify; propertyName and propertyType specify the property.\naccessibility defaults to \"public\". hasSetter=true generates { get; set; }; false generates { get; }.\nisInit=true generates { get; init; } (requires hasSetter=true).\nUse autoStage=true (default) to get a ChangeId for ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "containerName": {
          "type": "string"
        },
        "propertyName": {
          "type": "string"
        },
        "propertyType": {
          "type": "string"
        },
        "accessibility": {
          "type": "string",
          "default": "public"
        },
        "hasSetter": {
          "type": "boolean",
          "default": true
        },
        "isInit": {
          "type": "boolean",
          "default": false
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "containerName",
        "propertyName",
        "propertyType"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "extension_to_static",
    "description": "Converts an extension method into a standard static method call.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "invert_boolean_logic",
    "description": "Inverts all usages of a boolean identifier across the solution: wraps each usage with '!' and removes double negations. Returns a file-to-content map of changed files.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "boolName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "boolName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "add_summary_comment",
    "description": "Adds or replaces a /// <summary>...</summary> XML doc comment on a type or member.\n\ntargetName is the class/method/property name to document.\nsummaryText is the text content of the summary (single line).\nIf a summary already exists it will be replaced.\nUse autoStage=true (default) to get a ChangeId for ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "targetName": {
          "type": "string"
        },
        "summaryText": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "targetName",
        "summaryText"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "convert_to_switch",
    "description": "Replaces if-else chains with switch statements for better readability.\nConverts patterns like:\n- 'if (x == 1) {...} else if (x == 2) {...}' → 'switch (x) { case 1: ...; case 2: ... }'\n\nfilePath: path to the .cs file to transform.\nAutomatically detects and converts eligible if-else chains (minimum 3 branches).\nReturns the updated file content with switch statements applied.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "inline_class",
    "description": "Moves all members of a class into a target class and removes the source class declaration. Works within the same file (sourceFilePath == targetFilePath) or across files. Automatically updates ALL type references (variable declarations, constructor calls, casts, typeof, etc.) to the inlined class name throughout the solution, renaming them to the target class. Returns a dictionary of filePath→updatedContent for every affected file.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "sourceFilePath": {
          "type": "string"
        },
        "targetFilePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        }
      },
      "required": [
        "sourceFilePath",
        "targetFilePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_possible_deadlocks",
    "description": "Analyzes a solution or project for deadlocks. Optional scope.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "check_package_inconsistency",
    "description": "Checks for NuGet package version inconsistencies across multiple projects.",
    "inputSchema": {
      "type": "object",
      "properties": {}
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "flag_migration_candidates_batch",
    "description": "Flags multiple methods with [MigrationCandidate] attributes in a single call. Groups\r\nrewrites by file so each source file is read once and written once — avoids line-number\r\ndrift that occurs when sequential single-method calls modify the same file.\r\n\r\nitems: array of { FilePath, MethodName, Pattern, Score?, Reason? } objects.\r\n\r\nWrites all changes directly to disk. Returns FlagMigrationCandidatesBatchResult with\r\nper-method outcomes and any errors (items that failed do not abort the batch).\r\n\r\nPrefer flag_migration_candidates_in_project when you want autonomous discovery.\r\nUse this tool when you already have a specific list of methods to flag.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "items": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": "@{filePath=; methodName=; pattern=; score=; reason=}",
            "required": "filePath methodName pattern"
          }
        }
      },
      "required": [
        "items"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "format_document_preview",
    "description": "Returns a preview of what format_document would change without applying the changes.\nShows each changed line range with ±3 lines of context (similar to a unified diff).\nReturns Changed=false and an empty Hunks list if the file is already formatted correctly.\nUse this to inspect formatting changes before committing to format_document.\nfilePath: path to the .cs file to preview.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "check_project_consistency",
    "description": "Checks solution-wide consistency: TargetFramework alignment across projects and project naming convention adherence. Returns a list of issues with IssueType (TargetFrameworkMismatch, NamingConventionViolation), description, project name, and file path. NuGet package version consistency is covered by check_package_inconsistency.",
    "inputSchema": {
      "type": "object",
      "properties": {}
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "cleanup_implicit_spans",
    "description": "ImplicitSpanCleanup: Removes unnecessary .AsSpan() calls that are implicit in modern C#.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "list_features",
    "description": "Lists all available analysis/refactoring features and their current enabled status.",
    "inputSchema": {
      "type": "object",
      "properties": {}
    }
  },
  {
    "name": "find_all_throw_sites",
    "description": "Finds all throw sites (throw statements and throw expressions) across the solution, optionally filtered by exception type, file, or project. Returns file path, line, column, the exception type being thrown, the containing method name, whether the throw is inside a catch block (rethrow patterns), and any extracted message literal from the first argument. Set sortByFrequency=true to rank results by how often each exception type is thrown (most frequent first) — useful for auditing dominant error patterns.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "exceptionType": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "sortByFrequency": {
          "type": "boolean",
          "default": false
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "flag_migration_candidate",
    "description": "Adds a [MigrationCandidate(\"pattern\")] attribute to a method, marking it for a future\r\nasync migration refactoring pass. The attribute is source-injected — no new project\r\nreferences are required.\r\n\r\nWrites the change directly to disk — no apply_staged_changes or apply_proposed_changes\r\nstep needed. If MigrationCandidateAttribute.cs does not yet exist in the project it is\r\nalso generated in the same directory.\r\n\r\nKnown patterns: AsyncBridgeCandidate, HandlerExtract, HandlerToAsync, AsyncCallerUplift.\r\nRe-flagging a method with the same pattern is idempotent: the existing attribute is\r\nreplaced with the updated score/reason/date.\r\n\r\nThe specialist tool (e.g. convert_to_async_bridge) automatically removes [MigrationCandidate]\r\nwhen it processes the method. Remove the attribute manually if the method is ruled ineligible.\r\n\r\nReturns FlagMigrationCandidateResult with slim metadata only (no file source in the payload).\r\nWasAlreadyFlagged and PreviousPattern indicate idempotency state.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        },
        "pattern": {
          "type": "string"
        },
        "score": {
          "type": "integer",
          "default": 0
        },
        "reason": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "methodName",
        "pattern"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "detect_unused_private_fields",
    "description": "Detects private fields that are never read or written in the file.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "sync_type_and_filename",
    "description": "Synchronizes the filename to match the primary type declared within it.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_unused_private_members",
    "description": "Finds all unused private members in a class.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "upgrade_to_modern_guards",
    "description": "ModernGuardClauses: Upgrades if-throw null checks to ArgumentNullException.ThrowIfNull / ArgumentException.ThrowIfNullOrEmpty.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "extract_local_variable",
    "description": "Extracts an inline expression into a local variable declaration.\nConverts patterns like:\n- 'return x + y;' → 'var sum = x + y; return sum;'\n- 'var result = getValue();' → Extracts getValue() to a variable\n\ncontextSnippet: a verbatim substring containing or surrounding the expression to extract.\nvariableName: the name for the new variable.\nProvide lineBefore and/or lineAfter when the snippet could match multiple locations.\nReturns the updated file content with the expression extracted to a variable.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "contextSnippet": {
          "type": "string"
        },
        "variableName": {
          "type": "string"
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "contextSnippet",
        "variableName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "update_project_documentation",
    "description": "Writes (creates or overwrites) a project documentation file in the solution's docs/documentation/ directory. Filename is treated as bare — path components are stripped and rejected. Only documentation file types permitted. Content size capped at 512 KB. Cannot write source files or files outside the docs directory.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filename": {
          "type": "string"
        },
        "content": {
          "type": "string"
        }
      },
      "required": [
        "filename",
        "content"
      ]
    }
  },
  {
    "name": "generate_repository_interface",
    "description": "Extracts an interface from a concrete class. Returns: the interface source code ready to paste into a new file, a DI registration snippet (services.AddScoped<IFoo, Foo>()), and a Moq mock setup snippet for use in unit tests.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "fix_thread_sleep",
    "description": "EPC33: Replaces Thread.Sleep with Task.Delay in async methods.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "generate_decorator_class",
    "description": "Generates a Decorator pattern class for a given interface. The decorator wraps any implementation, delegates all interface members to an inner instance, and includes TODO comments for adding cross-cutting concerns (logging, caching, retry). Returns the full .cs file source.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "interfaceName": {
          "type": "string"
        },
        "decoratorPrefix": {
          "type": "string",
          "default": "Logging"
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "interfaceName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "introduce_variable",
    "description": "Introduces a new local variable based on a selected expression and replaces usages. Provide contextSnippet: a verbatim substring uniquely identifying the expression to extract. Provide lineBefore and/or lineAfter when the snippet could match multiple locations.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "contextSnippet": {
          "type": "string"
        },
        "newVariableName": {
          "type": "string"
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "contextSnippet",
        "newVariableName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "insert_member_before",
    "description": "Inserts a new member immediately before a named member in a type declaration.\n\nFinds the container type by name, then locates the member named beforeMemberName and inserts\nnewMemberSource directly before it (e.g. insert a field before a constructor).\nIf beforeMemberName is not found, the new member is appended at the end of the type.\nUse autoStage=true (default) to get a ChangeId for ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "containerName": {
          "type": "string"
        },
        "beforeMemberName": {
          "type": "string"
        },
        "newMemberSource": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "containerName",
        "beforeMemberName",
        "newMemberSource"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_duplicate_blocks_in_hierarchy",
    "description": "Finds duplicate statement sequences across all types in an inheritance or\ninterface hierarchy — base class, derived classes, and implementing types.\n\nScoped to avoid noise: only types that share a common ancestor or interface\nwith the named type are included. Cross-hierarchy clones (unrelated types)\nare excluded. This makes findings directly actionable: duplicates in a hierarchy\nare candidates for extraction to the base class or a shared abstract method.\n\nReturns the same DuplicateBlockGroup shape as FindDuplicateBlocksInClass,\nwith ContainingType and MethodName populated per occurrence so you can see\nexactly which derived class has the copy.\n\ntypeName: the root type to anchor the hierarchy search (class or interface name).\nprojectName: optional filter to limit the search scope.\nminStatements: minimum window size (default 4).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "typeName": {
          "type": "string"
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "minStatements": {
          "type": "integer",
          "default": 4
        }
      },
      "required": [
        "typeName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "insert_member_after",
    "description": "Inserts a new member immediately after a named member in a type declaration.\n\nFinds the container type by name, then locates the member named afterMemberName and inserts\nnewMemberSource directly after it (e.g. insert a new method after an existing method).\nIf afterMemberName is not found, the new member is appended at the end of the type.\nUse autoStage=true (default) to get a ChangeId for ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "containerName": {
          "type": "string"
        },
        "afterMemberName": {
          "type": "string"
        },
        "newMemberSource": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "containerName",
        "afterMemberName",
        "newMemberSource"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "convert_property_safe",
    "description": "Converts a property between auto-property and full property with backing field.\nUnlike the built-in convert_property, this correctly preserves initializers when converting\nToFullProperty (the initializer moves to the backing field) and handles virtual/override/new\nmodifiers without dropping them.\ndirection: \"ToFullProperty\" (auto-prop → backing field + expression-body accessors) or\n           \"ToAutoProperty\" (full property → auto-prop, moving backing field initializer to property).\npropertyName: the property to convert.\ncontextSnippet: optional verbatim substring to disambiguate when multiple properties share a name.\nProvide lineBefore and/or lineAfter when the snippet could match multiple locations.\nReturns the updated file content.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "propertyName": {
          "type": "string"
        },
        "direction": {
          "type": "string"
        },
        "contextSnippet": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "propertyName",
        "direction"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "make_method_static",
    "description": "Makes a method static if it does not access any instance members (fields, properties, methods).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "move_type_to_outer_scope",
    "description": "Moves a nested type out to the containing namespace scope.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "typeName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "typeName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "generate_http_client",
    "description": "Generates a typed HttpClient for a Web API controller.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "controllerName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "controllerName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "upgrade_pattern_matching",
    "description": "IDE0019/20: Upgrades legacy type checks and casts to modern C# pattern matching (is string s).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_code_inventory",
    "description": "Generates a structured report of all namespaces, classes, methods, and properties in a file.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_missing_cancellation_tokens",
    "description": "Finds async Task/ValueTask methods that lack a CancellationToken parameter but call at least one method that accepts one. These are methods that should thread cancellation through but aren't. Excludes: event handler methods with (object sender, XxxEventArgs e) signatures (delegate-bound, cannot accept CT), abstract methods, and methods that already have a CT parameter. Returns method name, containing type, file/line, and the callee names that accept CancellationToken.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_async_in_constructor",
    "description": "Finds constructors that call async methods or contain await expressions. Constructors cannot be async — use a factory method or AsyncHelper instead.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "detect_mismatched_await",
    "description": "Detects Task-returning method calls that are not awaited (potential fire-and-forget bugs). Skips intentional patterns: discard assignments (_ = MethodAsync()), Task.WhenAll/WhenAny arguments, return/arrow-body expressions, .GetAwaiter().GetResult() chains (those are blocking sync-over-async — use find_blocking_calls_in instead), ContinueWith/ConfigureAwait chains, field assignments, and variables later passed to WhenAll. Pass filePath to scope to a single file, projectName to scope to a project, or leave both null to scan the solution.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "preview_add_missing_usings",
    "description": "PREVIEW_ADD_MISSING_USINGS — Preview what usings would be added, without modifying the file.\n\nFIXES MS BUG: The standard add_missing_usings tool with preview:true silently APPLIES\nchanges to the file on disk anyway — the preview flag is completely ignored. Files are\nalways modified regardless of what preview value you pass.\n\nThis tool performs the analysis in read-only mode:\n  • Finds CS0246/CS0103 diagnostics (unresolved type/name errors)\n  • Searches all projects in the solution for matching type declarations\n  • Returns the list of namespaces that WOULD be added\n  • Returns UpdatedContent — the file content WITH the usings added (for review)\n  • Does NOT write to disk under any circumstances\n\nPrerequisites: A solution must be loaded (HasLoadedSolution=true from GetWorkspaceHealth).\nThe file must be part of the loaded solution.\n\nReturns: SolutionRequired, UsingsToAdd, Warning, UpdatedContent.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "detect_value_task_misuse",
    "description": "Detects invalid ValueTask usage patterns: (A) double await on same variable, (B) ValueTask stored and deferred-awaited with intervening statements, (C) ValueTask passed to Task.WhenAll() without .AsTask(), (D) .Result accessed on ValueTask. Reports method name and line for each violation.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "generate_mapping",
    "description": "Generates a mapping method between two types.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "fromType": {
          "type": "string"
        },
        "toType": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "fromType",
        "toType"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "optimize_to_value_task",
    "description": "Converts a method's Task/Task<T> return type to ValueTask/ValueTask<T> for reduced heap allocation on hot paths.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "read_handoff",
    "description": "Reads a session handoff document from the solution's docs/handoffs/ directory. Only bare filenames and documentation extensions are accepted.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filename": {
          "type": "string"
        }
      },
      "required": [
        "filename"
      ]
    }
  },
  {
    "name": "list_project_documentation",
    "description": "Lists all files in the solution's docs/ directory tree. Returns relative paths within docs/ only. Cannot enumerate source files or directories outside docs/.",
    "inputSchema": {
      "type": "object",
      "properties": {}
    }
  },
  {
    "name": "find_linq_n1_patterns",
    "description": "Detects LINQ N+1 query patterns: LINQ terminal calls (Where, FirstOrDefault, Any, Count, etc.) inside foreach/for/while loops where the loop variable appears in the LINQ chain — each iteration triggers a separate query. Reports file, line, and the loop variable involved.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "convert_expression_body",
    "description": "Converts a method or property between expression body (=>) and block body forms. direction: 'ToExpressionBody' or 'ToBlockBody'. memberName: the method/property name. contextSnippet: optional verbatim substring to disambiguate overloads. Provide lineBefore and/or lineAfter when the snippet could match multiple locations. Returns the updated file content.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "memberName": {
          "type": "string"
        },
        "direction": {
          "type": "string"
        },
        "contextSnippet": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "memberName",
        "direction"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "detect_layer_violations",
    "description": "Detects namespace-level layer architecture violations (e.g. Controllers importing from Data/Repositories directly, Domain models depending on infrastructure layers). Operates on using directives — fast, no compilation required. Returns violation type, description, source layer, forbidden dependency namespace, file path, and line. Scope with projectName or filePath, or omit for the entire solution.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_task_delay_zero_usage",
    "description": "Detects redundant Task.Delay(0) calls.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_large_switch_statements",
    "description": "Finds switch statements with a large number of cases that may need refactoring, optionally filtered by project.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "threshold": {
          "type": "integer",
          "default": 10
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "analyze_type_cohesion",
    "description": "Analyzes class cohesion using an LCOM-based metric. Returns per-class analysis including field/method counts, LCOM score (0=cohesive, 1=disconnected), a rating (Excellent/Good/Poor/Very Poor), and informal suggestions for classes that could be split. Useful for identifying god classes and extraction opportunities.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_linq_redundant_where",
    "description": "Detects LINQ chains of the form .Where(pred).First() / .Where(pred).Any() / .Where(pred).Count() where the intermediate Where creates an unnecessary IEnumerable allocation. Suggests collapsing to .First(pred) / .Any(pred) / .Count(pred) for a single-pass, allocation-free alternative.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "trace_variable_lifetime",
    "description": "Traces a variable's complete lifetime from declaration through every read, write, ref/out pass,\nreturn, and closure capture, across all code paths (loops, conditionals, try/catch) in the\nenclosing scope.\nfilePath: file containing the variable declaration.\nvariableName: the exact identifier name.\nlineNumber: 1-based line of the declaration (used to disambiguate variables with the same name).\nReturns: TypeName, DeclarationLine, ScopeDescription, IsDefinitelyAssigned, IsAlwaysAssigned,\nIsCapturedInClosure, and an Accesses list with Line, Column, AccessKind (Declaration/Read/Write/\nRef/Out/Return/Capture), ContextStack (method > if > for ancestry), IsInLoop, IsInConditional.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "variableName": {
          "type": "string"
        },
        "lineNumber": {
          "type": "integer"
        }
      },
      "required": [
        "filePath",
        "variableName",
        "lineNumber"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_unused_interfaces",
    "description": "Identifies interfaces that are declared but never implemented in the solution or a specific project.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "propagate_cancellation_token_in_method",
    "description": "Propagates an existing CancellationToken parameter from a method to all eligible\r\nasync callees within its body. The target method must already have a CancellationToken\r\nparameter. For each call to an async method that has a CancellationToken overload but\r\nisn't receiving the token, appends the token to the call site. Call sites that already\r\nforward a CT or call methods without CT overloads are reported as skipped.\r\nReturns a staged change-set; call apply_staged_changes to commit.\r\n\r\nParameters:\r\n  filePath   — absolute path to the source file containing the method.\r\n  methodName — name of the method to process (case-sensitive).\r\n  autoStage  — when true (default), stages the changes and returns a ChangeId.\r\n               When false, returns the updated source text with no ChangeId.\r\n\r\nReturns PropagateCtResult with:\r\n  MethodFound        — false if the method was not found in the file.\r\n  TokenParameterName — actual parameter name used (e.g. \"ct\" or \"cancellationToken\").\r\n  Forwarded          — call sites that were rewritten with the token.\r\n  Skipped            — call sites skipped with reason code.\r\n  ForwardedCount     — count of Forwarded.\r\n  SkippedCount       — count of Skipped.\r\n  ChangeId           — staged change id (null if autoStage=false or no changes).\r\n  Error              — non-null on hard failure (method has no CT param, etc.).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "add_member_to_class",
    "description": "Adds a new member (method, field, etc.) to an existing class, interface, record, or struct.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "containerName": {
          "type": "string"
        },
        "newMemberSource": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "containerName",
        "newMemberSource"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "inline_field",
    "description": "Inlines a private field by replacing all usages with its initializer value.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "fieldName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "fieldName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_check_then_act_on_dictionary",
    "description": "Detects the check-then-act race on Dictionary/ConcurrentDictionary: ContainsKey() or TryGetValue() followed by Add()/TryAdd() on the same variable outside of any lock. Another thread may insert the same key between the check and add. Use GetOrAdd() or TryAdd() for atomic insertion.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "convert_method_to_indexer",
    "description": "Converts a Get(index) method into a formal C# indexer.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_long_parameter_list",
    "description": "Finds methods/constructors with >= minParameters (default 4) parameters. Excludes DI-only constructors (all params end with Service/Repository/Options/Factory).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "minParameters": {
          "type": "integer",
          "default": 4
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "upgrade_thread_safety",
    "description": "Upgrades threading patterns (locks, semaphores) to modern/safe versions.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "split_project_by_folder",
    "description": "Moves all files under a specific folder from a source project to a new target project, preserving folder structure.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "sourceProjectName": {
          "type": "string"
        },
        "folderName": {
          "type": "string"
        },
        "targetProjectName": {
          "type": "string"
        }
      },
      "required": [
        "sourceProjectName",
        "folderName",
        "targetProjectName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_callers_safe",
    "description": "Finds all call sites (references) to a symbol in the solution without requiring line/column coordinates.\nUnlike the built-in find_callers which needs a line number, this locates the symbol by name with\nan optional contextSnippet to disambiguate overloads.\nsymbolName: the method/property/field name to search for.\ncontextSnippet: optional verbatim substring of the declaration (e.g. the method signature line).\nProvide lineBefore and/or lineAfter when the snippet could match multiple locations.\nReturns CallerMethod, CallerType, FilePath, Line, and CodeSnippet for each call site.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "symbolName": {
          "type": "string"
        },
        "contextSnippet": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "symbolName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "add_using_directive",
    "description": "Adds a using directive to a file if not already present.\n\nPass just the namespace name (e.g. \"System.Linq\", \"Microsoft.Extensions.DependencyInjection\").\nFor static usings, prefix with \"static \" (e.g. \"static System.Math\").\nIf the directive already exists, the file is returned unchanged.\nUse autoStage=true (default) to get a ChangeId for ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "namespaceName": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "namespaceName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "extract_method_safe",
    "description": "extract_method_safe— extracts selected statements into a new method with the CORRECT return type.\n\nFixes a bug in the standard extract_method tool where selections ending with `return <expression>`\nare extracted into a method declared `private void MethodName(...)`. The void return type is\nincorrect and causes a compile error. This tool uses Roslyn's SemanticModel to determine the\nactual type of the returned expression, and DataFlowAnalysis to find the correct parameter list.\n\nRequires a solution to be loaded first (via set_solution_path or equivalent), because semantic\nanalysis is needed to determine types.\n\nParameters:\n  filePath        — absolute path to the .cs file (must be in the loaded solution)\n  newMethodName   — valid C# identifier for the new extracted method\n  contextSnippet  — a short unique code snippet that identifies the selection\n  lineBefore      — optional: the line immediately before contextSnippet (for disambiguation)\n  lineAfter       — optional: the line immediately after contextSnippet (for disambiguation)\n\nReturns: MsAugmentResult with Success=true and UpdatedContent=the rewritten file,\n         or Success=false and Error with a human-readable explanation.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "newMethodName": {
          "type": "string"
        },
        "contextSnippet": {
          "type": "string"
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "newMethodName",
        "contextSnippet"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_blast_radius",
    "description": "Gets the blast radius (impact analysis) of changing a symbol. Provide contextSnippet: a verbatim substring from the symbol's declaration or reference (e.g., 'public async Task<T> GetById('). Provide lineBefore and/or lineAfter when the snippet could match multiple locations. Returns all call sites and affected projects.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "contextSnippet": {
          "type": "string"
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "contextSnippet"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "add_configure_await_false",
    "description": "Adds .ConfigureAwait(false) to all await expressions in a file that don't already have it. Use libraryMode=true (default) for library code, false for ASP.NET app code. Idempotent — skips already-configured awaits. Returns the updated source as a string. Does NOT write to disk or update the workspace. Pass the result to apply_proposed_changes to save.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "libraryMode": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "extract_constant_safe",
    "description": "EXTRACT_CONSTANT_SAFE — Extract a literal to a named constant using context, not line/column.\n\nFIXES MS BUG: The standard extract_constant tool requires exact 1-based line/column\ncoordinates. When these are even slightly wrong (off by one, trailing whitespace,\nCRLF vs LF differences) it throws a cryptic error: \"Column 99 is beyond end of line\".\nThere is no human-readable guidance on how to fix the coordinates.\n\nThis tool uses contextSnippet to locate the literal — the same approach used by all\nother safe Sentinel tools:\n  • No line/column arithmetic required\n  • Human-readable error messages when the literal cannot be found\n  • Replaces ALL identical literals in the file (not just the one occurrence)\n  • Inserts the constant as the first member of the containing type\n\nParameters:\n  filePath        — absolute path to the .cs file\n  contextSnippet  — the literal itself or a short surrounding snippet (e.g., '\"hello world\"')\n  constantName    — valid C# identifier for the constant (e.g., 'GreetingMessage')\n  lineBefore      — optional: the line immediately before contextSnippet (for disambiguation)\n  lineAfter       — optional: the line immediately after contextSnippet (for disambiguation)\n\nReturns: MsAugmentResult with Success=true and UpdatedContent=the rewritten file,\n         or Success=false and Error with a human-readable explanation.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "contextSnippet": {
          "type": "string"
        },
        "constantName": {
          "type": "string"
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "contextSnippet",
        "constantName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "rename_symbol",
    "description": "Renames a symbol (class, method, property, field, local, etc.) across the entire solution. Pass 'symbolName' (the exact identifier to rename) and 'contextSnippet' (a verbatim substring from the source file, long enough to appear exactly once — typically the surrounding line or expression). Example: symbolName=\"GetById\", contextSnippet=\"public async Task<Product?> GetById(\". Provide lineBefore and/or lineAfter (verbatim text from the line above/below the target) when the snippet could match multiple locations. Returns an error if the snippet matches zero or multiple locations. Returns per-file diff hunks (before/after for each changed line with ±2 lines of context) plus a staged ChangeId. Review FileChanges before calling ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "symbolName": {
          "type": "string"
        },
        "contextSnippet": {
          "type": "string"
        },
        "newName": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "symbolName",
        "contextSnippet",
        "newName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_misbound_overload_chains",
    "description": "Validates overload chain correctness across the solution. For each group of same-named methods where shorter overloads delegate to fuller ones, detects: (1) ChainMissingParameter — a parameter from the calling overload is absent from the forwarding call, silently dropping the caller's value; (2) ChainArgumentOrder — source parameters appear in inverted order in the forwarding call, binding to the wrong target parameter; (3) OverloadCycle — two overloads delegate to each other, causing guaranteed StackOverflowException. Requires a loaded solution.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "extract_superclass",
    "description": "Extracts common members from multiple classes into a new shared base class.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePaths": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "classNames": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "newBaseClassName": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePaths",
        "classNames",
        "newBaseClassName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "remove_modifier",
    "description": "Removes a modifier keyword from a type or member (idempotent).\n\ntargetName is the class/method/property name to modify.\nmodifier is the keyword to remove: virtual, abstract, sealed, static, readonly, override,\npartial, async, new, extern, unsafe, volatile.\nUse autoStage=true (default) to get a ChangeId for ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "targetName": {
          "type": "string"
        },
        "modifier": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "targetName",
        "modifier"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "safe_delete_symbol",
    "description": "Safe deletes a symbol only if it has zero usages in the entire codebase. Provide contextSnippet: a verbatim substring from the symbol's declaration line. Provide lineBefore and/or lineAfter (verbatim text from the line above/below the target) when the snippet could match multiple locations. If autoStage is true, returns a ChangeId.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "contextSnippet": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "contextSnippet"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_multiple_out_parameter_methods",
    "description": "Finds methods with 2 or more 'out' parameters — a common sign that the method\r\nis doing the work of returning multiple values through side-channels instead of\r\na proper return type.\r\n\r\nReturns the current return type, the out-parameter names and types, and a suggested\r\nValueTuple signature. Use ConvertOutParamsToValueTuple to apply the conversion\r\nautomatically once you've reviewed the findings.\r\n\r\nScope to a file or project, or scan the entire solution.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "replace_member",
    "description": "Surgically replaces a specific member (method, property, class) in a file by name with new source code.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "memberName": {
          "type": "string"
        },
        "newSource": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "memberName",
        "newSource"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_task_when_all_usage",
    "description": "Detects sequential await calls that could be parallelized.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_uninstantiated_types",
    "description": "Identifies classes that are never instantiated across the entire solution or a specific project.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "detect_unreachable_code",
    "description": "Detects statements within a specific method that are unreachable due to a preceding return, throw, break, or continue on all code paths. Returns string descriptions of each unreachable statement and the reason it cannot execute. Use before adding code at the end of a method to confirm the insertion point is actually reached.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "extract_members_to_partial",
    "description": "Extracts specific members from a class into a new partial file.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        },
        "memberNames": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "filePath",
        "className",
        "memberNames"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "upgrade_unbound_nameof",
    "description": "IDE0340: Upgrades nameof(List<int>) to modern C# 14 nameof(List<>). Provide contextSnippet: the verbatim nameof expression text. Provide lineBefore and/or lineAfter when the snippet could match multiple locations.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "contextSnippet": {
          "type": "string"
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "contextSnippet"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "inline_variable",
    "description": "Inlines a local temporary variable into all its usages within the scope.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "variableName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "variableName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "preview_rename_impact",
    "description": "Previews the impact of renaming a symbol across the solution without applying changes. Returns affected files and location count. symbolName: the current name. contextSnippet: optional verbatim substring to disambiguate. Provide lineBefore and/or lineAfter when the snippet could match multiple locations.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "symbolName": {
          "type": "string"
        },
        "contextSnippet": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "symbolName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_feature_status",
    "description": "Gets the enabled status of specific features by name.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "featureNames": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "featureNames"
      ]
    }
  },
  {
    "name": "analyze_security",
    "description": "Analyzes a file for potential security vulnerabilities.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "load_solution",
    "description": "Loads a .NET solution into memory for persistent analysis.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "solutionPath": {
          "type": "string"
        }
      },
      "required": [
        "solutionPath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "update_features",
    "description": "Batch updates the enabled status of one or more features.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "updates": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": "@{key=; value=}",
            "required": "key value"
          }
        }
      },
      "required": [
        "updates"
      ]
    }
  },
  {
    "name": "fix_mismatched_namespaces",
    "description": "Corrects the namespace declaration in a file to match its actual folder structure relative to the project root.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_task_delay_usage",
    "description": "Detects Task.Delay() usage.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_unbounded_static_collections",
    "description": "Detects static fields that hold unbounded collections (Dictionary, List, HashSet, etc.) that are populated with .Add()/.TryAdd() but never .Clear()ed and have no Count-based size cap. A common memory exhaustion DoS vector when populated from user-controlled data.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "wrap_in_try_catch",
    "description": "Wraps a range of statements in a try/catch block.\n\nstartLine and endLine are 1-based line numbers of the statements to wrap.\nexceptionType defaults to \"Exception\"; catchVariableName defaults to \"ex\".\ncatchBody is an optional statement for the catch block body (e.g. \"_logger.LogError(ex, \\\"msg\\\");\").\nUse autoStage=true (default) to get a ChangeId for ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "startLine": {
          "type": "integer"
        },
        "endLine": {
          "type": "integer"
        },
        "exceptionType": {
          "type": "string",
          "default": "Exception"
        },
        "catchVariableName": {
          "type": "string",
          "default": "ex"
        },
        "catchBody": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "startLine",
        "endLine"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "wrap_in_using",
    "description": "Wraps a range of code in a using statement for an IDisposable object.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "startLine": {
          "type": "integer"
        },
        "endLine": {
          "type": "integer"
        },
        "disposalName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "startLine",
        "endLine",
        "disposalName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_diagnostics_summary",
    "description": "Returns a grouped summary of Roslyn compiler diagnostics (errors and warnings) for a file,\r\nproject, or the entire solution. Groups by diagnostic ID (e.g. CS0103) so you can see which\r\nissues are most common without scrolling through hundreds of raw messages.\r\nfilePath: analyze a single file (mutually exclusive with projectName).\r\nprojectName: analyze all files in a project.\r\nLeave both null to analyze the entire solution.\r\ntopN: max number of groups to return, sorted by count descending (default 20).\r\nReturns TotalIssues, Errors, Warnings, and a TopIssues list with DiagnosticId, Severity,\r\nMessageTemplate, Count, and Locations.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "topN": {
          "type": "integer",
          "default": 20
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "simplify_boolean_expressions",
    "description": "Simplifies boolean expressions: rewrites 'x == true' to 'x', 'x == false' to '!x', and similar redundant patterns.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_implicit_nullable_boxing",
    "description": "Detects explicit casts of Nullable<T> values to object or dynamic, which box the nullable and can produce surprising null-equality behavior ((object)(int?)null == null is true, but two boxed int? values with the same value are not reference-equal). Uses the semantic model for accuracy.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_mutable_public_collection_properties",
    "description": "Detects public properties that expose mutable collection types (List<T>, Dictionary<K,V>, HashSet<T>, etc.) with a public non-init setter, allowing callers to completely replace the collection. Use IReadOnlyList<T>/IReadOnlyDictionary<K,V>, or change the setter to private/init.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "run_uplift_batch_multi",
    "description": "Runs run_uplift_batch for each bridged method in the provided list, collecting\r\nper-method and aggregate results. Processes all targets sequentially; a failure on\r\none method does not block the others.\r\n\r\nInput (UpliftBatchMultiInput):\r\n  Targets                   — list of { BridgedMethodName, ProjectName? } targets.\r\n  MaxCallersPerMethod       — max callers to process per bridged method (default 10).\r\n  DryRun                    — when true, reports what would happen without writing files.\r\n  PropagateCancellationTokens — when true (default), propagates CT to other async\r\n                                callees in each newly created async overload.\r\n\r\nReturns UpliftBatchMultiResult with:\r\n  PerMethod            — one UpliftBatchMultiMethodResult per target, each containing\r\n                         the full UpliftBatchResult for that method plus any Error.\r\n  TotalUplifted        — total callers uplifted across all methods.\r\n  TotalSkipped         — total callers skipped across all methods.\r\n  TotalRemainingCallers — total pending callers not yet processed (budget cap).\r\n  StopReason           — \"batch_complete\" | \"dry_run\".",
    "inputSchema": {
      "type": "object",
      "properties": {
        "input": {
          "type": "object",
          "properties": {
            "targets": "@{type=array; items=}",
            "maxCallersPerMethod": "@{type=integer}",
            "dryRun": "@{type=boolean}",
            "propagateCancellationTokens": "@{type=boolean}"
          }
        }
      },
      "required": [
        "input"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "convert_to_async_enumerable",
    "description": "Converts a method returning Task<List<T>>, Task<IEnumerable<T>>, or List<T> to IAsyncEnumerable<T>. Transforms 'results.Add(item)' patterns to 'yield return item', removes the list variable and return statement, and adds a CancellationToken parameter if missing. Returns the updated source as a string. Does NOT write to disk or update the workspace. Pass the result to apply_proposed_changes to save.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "analyze_method_data_flow",
    "description": "Analyzes data flow for an entire method body using Roslyn's semantic analysis.\r\n\r\nUnlike the raw line-based analyze_data_flow tool, this takes a method name — no\r\nneed to count line numbers or worry about accidentally including the method signature.\r\n\r\nReturns: DataFlowsIn (variables read but declared outside), DataFlowsOut (variables\r\nassigned inside and read outside), VariablesDeclared (locals), AlwaysAssigned (always\r\ninitialized on all code paths), ReadInside, WrittenInside.\r\n\r\nIf the method has multiple overloads, supply disambiguateLine (any line number that\r\nfalls inside the desired overload's body) to select the correct one.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        },
        "disambiguateLine": {
          "type": [
            "integer",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "remove_configure_await_false",
    "description": "Removes all .ConfigureAwait(x) calls from a file, leaving the bare awaited expression. Useful when migrating library code to ASP.NET app code where ConfigureAwait is unnecessary. Returns the updated source as a string. Does NOT write to disk or update the workspace. Pass the result to apply_proposed_changes to save.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "analyze_foreach_for_linq_conversion",
    "description": "ANALYZE_FOREACH_FOR_LINQ_CONVERSION — Pre-flight safety analysis for convert_foreach_linq.\n\nFIXES MS BUG: The standard convert_foreach_linq tool silently destroys data.\nWhen a collection is modified BEFORE the foreach (e.g., results.Add(\"header\") before\nthe loop), the standard tool re-initializes the variable with 'new List<T>()',\ndiscarding those pre-loop additions WITHOUT any warning.\n\nALWAYS call this tool before using convert_foreach_linq.\nOnly proceed with conversion if IsSafeToConvert=true.\n\nParameters:\n  filePath        — absolute path to the .cs file\n  contextSnippet  — a short snippet of the foreach statement (e.g., \"foreach (var item in\")\n  lineBefore      — optional: the line of code immediately before contextSnippet\n  lineAfter       — optional: the line of code immediately after contextSnippet\n\nReturns: IsSafeToConvert, CollectionVariableName, StatementsBeforeForeach, BlockingReason,\n         and a Recommendation.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "contextSnippet": {
          "type": "string"
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "contextSnippet"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_staged_changes",
    "description": "Returns the full file content of a staged change set for inspection.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "changeId": {
          "type": "string"
        }
      },
      "required": [
        "changeId"
      ]
    }
  },
  {
    "name": "convert_switch_to_expression",
    "description": "IfToSwitch: Converts a switch statement in a method to a switch expression.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "convert_static_to_extension",
    "description": "Converts a standard static method into an extension method by prepending 'this' to its first parameter.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "detect_json_anti_patterns",
    "description": "Detects unsafe or incorrect System.Text.Json usage patterns in a file: (1) JsonDocument.Parse() not wrapped in a 'using' — leaks pooled memory back to the ArrayPool, (2) JsonElement.GetProperty() instead of TryGetProperty() — throws KeyNotFoundException on missing keys at runtime. Returns SecurityIssueReport with IssueType, file path, line/column, and remediation description.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "generate_to_string_safe",
    "description": "generate_tostring_safe — generates a ToString() override with CORRECTLY ESCAPED interpolated strings.\n\nFixes a bug in the standard generate_tostring tool where literal { characters in the format section\nare left unescaped, producing broken code like $\"Type { Prop = {Prop} }\" that fails with\nCS8086 (invalid interpolation hole). This tool always emits {{ and }} for literal braces,\nproducing valid code like $\"Type {{ Prop = {Prop} }}\".\n\nParameters:\n  filePath  — absolute path to the .cs file\n  typeName  — the class/struct to add ToString() to\n  members   — optional comma-separated list of property/field names to include;\n              if omitted, all public instance properties and fields are used\n\nReturns: MsAugmentResult with Success=true and UpdatedContent=the rewritten file,\n         or Success=false and Error with a human-readable explanation.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "typeName": {
          "type": "string"
        },
        "members": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "typeName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_all_implementations",
    "description": "Finds all types that implement an interface or derive from a class, returning file path and line for each. Optionally scoped to a single project.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "typeName": {
          "type": "string"
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "typeName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "analyze_stack_overflow_risks",
    "description": "Statically analyzes a C# file for potential StackOverflowException causes.\r\n\r\nDetects six distinct categories:\r\n- DirectRecursion       (Definite)   — method calls itself with no conditional guard\r\n- OverrideCallsSelf     (Definite)   — override calls own name instead of base.X()\r\n- PropertySelfRead      (Definite)   — property getter reads itself (missing backing field)\r\n- PropertySelfWrite     (Definite)   — property setter assigns to itself\r\n- InheritanceCycle      (Definite/Suspicious) — override → base → virtual dispatch → same override\r\n- ConditionalRecursion  (Suspicious) — guarded self-call whose base case may be unreachable\r\n- ArgumentNotDecreasing (Suspicious) — recursive call passes parameter unchanged or growing\r\n- MutualRecursion       (Suspicious) — A calls B calls ... calls A (up to 5-hop cycles)\r\n- DeepCallChain         (Informational, opt-in) — >40 static frames deep\r\n\r\nInheritance cycle detection covers both in-file class hierarchies (syntactic) and\r\ncross-file base classes (semantic). The most dangerous case — override calls base which\r\ncalls an abstract/virtual that this class overrides, creating a hidden dispatch loop —\r\nis detected with full CyclePath tracing.\r\n\r\nincludeInformational: set true to include DeepCallChain findings (default: false)",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "includeInformational": {
          "type": "boolean",
          "default": false
        }
      },
      "required": [
        "filePath"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "analyze_switch_for_pattern_conversion",
    "description": "Pre-flight analysis: checks whether a switch statement is safe to convert to a\nswitch expression using the standard 'convert_to_pattern_matching' tool.\n\nWHY YOU NEED THIS: The standard tool silently drops variable assignments in switch\ncases that assign to multiple variables. For example:\n  case \"g\":\n    totalOz   = rawValue / 28.3495m;   // ← DROPPED by standard tool\n    totalGrams = rawValue;               // ← only this survives\n    break;\nThis produces broken, data-loss code with no warning.\n\nThis tool returns a full case-by-case analysis, tells you which cases have multiple\nassignments, and gives a clear blocking reason when conversion is unsafe.\n\nIf IsSafeToConvert is true, the standard tool (or ConvertSwitchToPatternSafe) will\nproduce correct output.\n\nParameters:\n  filePath       - Absolute path to the .cs file.\n  contextSnippet - A verbatim substring from the switch keyword or governing\n                   expression line, e.g. \"switch (unit)\" or \"switch (rawUnit)\".\n  lineBefore/lineAfter - Verbatim text from the line above/below the target to\n                   disambiguate when the snippet matches multiple locations.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "contextSnippet": {
          "type": "string"
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "contextSnippet"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "change_signature",
    "description": "Reorders parameters in a method signature and updates all call sites globally.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        },
        "newParameterOrder": {
          "type": "array",
          "items": {
            "type": "integer"
          }
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "methodName",
        "newParameterOrder"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "convert_to_async_bridge",
    "description": "Converts a synchronous method to the Asyncify-bridge pattern in one atomic step.\r\n\r\nThree things happen:\r\n  1. A new async overload '<methodName>Async' is inserted immediately after the original.\r\n     It has the original body copied verbatim (NOT a Task.Run wrapper or scaffold),\r\n     'CancellationToken cancellationToken = default' appended as the last parameter,\r\n     and the 'async' modifier added. Expression-bodied originals are converted to block\r\n     bodies so downstream CT-propagation tools can operate on them.\r\n  2. The original sync method body is replaced with the bridge call:\r\n       return <methodName>Async(params...).GetAwaiter().GetResult();\r\n     (bare expression statement for void-returning methods)\r\n  3. [Obsolete(\"Asyncify-bridge: call <methodName>Async instead.\", false)] is added\r\n     to the original sync method — CS0618 warnings at call sites are the migration\r\n     tracking mechanism.\r\n\r\nThe async body is NOT further transformed. Use apply_cancellation_token_to_file or\r\nadd_cancellation_token_to_method in a follow-up step to propagate the CancellationToken\r\nto inner async callees.\r\n\r\nReturns CancellationTokenResult where exactly one field is populated:\r\n  autoStage=true (default): ChangeId is set; apply with apply_staged_changes(changeId).\r\n  autoStage=false: Source is set; pass Source.UpdatedSource to apply_proposed_changes.\r\n\r\nHard preconditions (throws if any fail): method must not already be async; must not\r\nend with 'Async'; must not be abstract; must not be an event handler; must not have\r\nref/out parameters; '<methodName>Async' must not already exist in the class.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "generate_constructor",
    "description": "Generates a constructor for a class from its private/readonly fields. Skips if a constructor already exists. Returns the updated file content.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "add_enum_value",
    "description": "Adds a new value to an existing enum declaration.\n\nSpecify the enum name and the new value name. Optionally provide an explicit integer value\n(e.g. enumName=\"Status\", valueName=\"Archived\", explicitValue=99 produces 'Archived = 99').\nIf the enum is not found in the file, the file is returned unchanged.\nUse autoStage=true (default) to get a ChangeId for ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "enumName": {
          "type": "string"
        },
        "valueName": {
          "type": "string"
        },
        "explicitValue": {
          "type": [
            "integer",
            "null"
          ],
          "default": null
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "enumName",
        "valueName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "add_benchmark_stub",
    "description": "Adds a BenchmarkDotNet stub class for performance testing a specific method. Returns the updated source as a string. Does NOT write to disk or update the workspace. Pass the result to apply_proposed_changes to save.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "className": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        }
      },
      "required": [
        "filePath",
        "className",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "add_attribute",
    "description": "Adds an attribute to a named type or member.\n\ntargetName is the class name or method/property name to decorate.\nattributeSource is the attribute text with or without brackets (e.g. \"[ApiController]\" or \"Required\").\nComplex attributes with arguments are supported (e.g. \"[HttpGet(\\\"/api/items\\\")]\").\nUse autoStage=true (default) to get a ChangeId for ApplyStagedChanges.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "targetName": {
          "type": "string"
        },
        "attributeSource": {
          "type": "string"
        },
        "autoStage": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "filePath",
        "targetName",
        "attributeSource"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_mutable_public_properties",
    "description": "Finds public mutable properties (public setter) on non-DTO public classes. Reports classes that expose state directly rather than through controlled mutation. Classes whose names end with Request/Response/Dto/ViewModel/Model/Options/Settings/Config/Entity/Event/Command/Query are excluded. Scope to a file or project, or scan the whole solution.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "convert_string_format_to_interpolated_smart",
    "description": "Converts a string.Format() call to an interpolated string.\n\nFIXES standard 'convert_to_interpolated_string' LIMITATION: The standard tool\nfails with \"First argument must be a string literal\" when the format string is a\nnamed constant or static field, e.g.:\n  string.Format(CacheKeyFmt, userId, date)\n  string.Format(ErrorMessages.NotFound, id)\n\nThis tool uses the semantic model to resolve the constant value at compile time,\nthen builds the interpolated string from the resolved format pattern.\n\nSupported format string sources:\n  • Inline string literal:   string.Format(\"User {0}\", id)\n  • const field/property:    string.Format(MyConst, id)\n  • Static readonly string:  string.Format(Templates.Key, id) (if const)\n\nSupports format specifiers:   string.Format(\"{0:yyyy-MM-dd}\", date) → $\"{date:yyyy-MM-dd}\"\n\nParameters:\n  filePath       - Absolute path to the .cs file.\n  contextSnippet - A verbatim substring from the string.Format call, e.g.\n                   \"string.Format(CacheKeyFmt\" or \"Format(MyErrorTemplate,\".\n  lineBefore/lineAfter - Verbatim text from the line above/below the target to\n                   disambiguate when the snippet matches multiple locations.\n\nReturns: UpdatedContent with the interpolated string, or Error if the format\nstring could not be resolved to a compile-time constant.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "contextSnippet": {
          "type": "string"
        },
        "lineBefore": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "lineAfter": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "filePath",
        "contextSnippet"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "find_attribute_usages",
    "description": "Finds all usages of a named attribute across the solution, optionally scoped to a\nproject or file. Accepts both \"Authorize\" and \"AuthorizeAttribute\" (or \"[Authorize]\")\nspelling — all three resolve identically.\n\nReturns: AttributeName, TargetKind (Class/Method/Property/Field/Parameter/Constructor/\nInterface/Record/Struct/Enum), TargetName, ContainingType (empty for type-level attrs),\nFilePath, and Line.\n\nCommon use cases:\n  • Security audit: find every endpoint missing [Authorize] by listing controllers and\n    diffing against this result.\n  • API review: \"find_attribute_usages [ProducesResponseType]\" to see which actions\n    document their return types.\n  • Validation audit: \"find_attribute_usages [Required]\" to map all required properties.\n  • Obsolete API detection: \"find_attribute_usages [Obsolete]\" to find every member\n    still marked deprecated.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "attributeName": {
          "type": "string"
        },
        "projectName": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "filePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "attributeName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  },
  {
    "name": "get_reverse_call_graph",
    "description": "Builds a reverse call graph (who calls this method): shows all methods that call the given method, what calls those, etc., up to maxDepth levels. Uses Roslyn SymbolFinder for accurate semantic reference resolution — not text search. Returns a ReverseCallGraphNode tree: MethodName, ContainingType, FilePath, Line, Callers.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string"
        },
        "methodName": {
          "type": "string"
        },
        "maxDepth": {
          "type": "integer",
          "default": 3
        }
      },
      "required": [
        "filePath",
        "methodName"
      ]
    },
    "execution": {
      "taskSupport": "optional"
    }
  }
]
