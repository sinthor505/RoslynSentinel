# Handle text files automatically (detect type and normalize to LF in repo)
* text=auto

# Explicitly declare files that must maintain specific line endings
*.sh text eol=lf
*.bat text eol=crlf
*.ps1 text eol=crlf

# Denote binary files so Git leaves them completely untouched
*.png binary
*.jpg binary
